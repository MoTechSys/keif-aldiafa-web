# PLAN.md — خطة إعادة بناء موقع «كيف الضيافة» على Next.js

> **هذا الملف هو المرجع الوحيد لأي وكيل أو جلسة جديدة.** اقرأه كاملاً قبل أي عمل.
> آخر تحديث: 2026-09-09 · الحالة: **المراحل 0–6 ✅ مبنية ومفحوصة وموثّقة (D109–D146): 43 مساراً · Playwright 168/168 · axe 0 · HTML 43/43 · guard ✅** · التالي: **قرارات المالك (مراجعة الصور D139 · مقاسات D138) ← أمر النشر ← المرحلة 7**. التسليم الدقيق: `docs/AGENT-STATE.md`
> عند أي تعارض بين هذا الملف وملف آخر في هذا المستودع (CLAUDE.md، README، التقارير القديمة) → **هذا الملف يفوز**.

---

## 0. المالك وأسلوبه

- **MoTechSys** — يُنادَى «يا غالي». يكتب بالعامية اليمنية/السعودية وبالصوت («اشتي» = أريد، «سوي» = افعل، «اوك» = تمام). **فسّر النية لا الحرف.**
- يفوّض بالكامل: «قرّر أنت ووثّق». يريد ردوداً **عربية موجزة**، بعناوين، بأرقام مقاسة، و**سؤالاً واحداً فقط** عند الضرورة. الأسئلة المعقدة تربشه — اسأل بسيطاً: نعم/لا أو (أ)/(ب).
- 99% من عملائه على الجوال. الهدف الوحيد للموقع: **أن يتواصل الزائر عبر واتساب**.
- الأولوية المطلقة عنده: **الفهرسة وأداء Google وفهم نية الباحث** — «موقع متمكن، ليس موقعاً عادياً».

---

## 1. المستودعات الخمسة — الأدوار (لا تخلط بينها)

| # | المستودع | الدور | الصلاحية |
|---|---|---|---|
| 1 | **`MoTechSys/keif-aldiafa-web`** (هذا) | **مستودع العمل** — Next.js 14 App Router. هنا يُبنى الموقع الجديد. | ✍️ كتابة — `main` مباشرة |
| 2 | **`MoTechSys/allpro`** → `prototype-home/` | **المصدر الفني للهيكلية والتصميم** (v6.9): 43 صفحة HTML مولَّدة + `build/build.py` + `build/local.py` + `build/data.py` (15 خدمة، 76 صنف تقديمات) + خطوط woff2 محلية + CSS مضمّن (418 سطر) | 👁️ قراءة فقط |
| 3 | **`MoTechSys/catalog-keif-aldiafa-photots`** | **مصدر الحقيقة للصور**: `catalog/catalog.json` (325 سجل) + `web/` (314 صورة محسّنة ≤1200px/≤180KB **بالشعار مدموج**) + `logos/web/` (49 شعار شريك بلا علامة) + `logos/partners.json` | 👁️ قراءة فقط |
| 4 | **`MoTechSys/keif-v2`** | **الإنتاج الحي** على Vercel (keifaldiafa.com) | ⛔ لا يُلمس إلا بأمر نشر صريح في النهاية |
| 5 | `MoTechSys/khadamat` | مرآة قديمة لـ prototype-home | 🚫 تجاهله |

**قرار D114:** لا مستودع رابع جديد. العمل كله في (1)، والنسخة القديمة محفوظة في فرع `old-version-2026-09-07`.

---

## 2. ما هو الوضع الآن (مُقاس 2026-09-07)

### 2.1 المستودع الحالي (keif-aldiafa-web)
- **44 صفحة** حقيقية: 24 (3 خدمات × 8 مدن) + 8 مدن + 1 نية (`/mubashirin-qahwa-jeddah`) + 11 عامة.
- بوابة الجودة: tsc ✅ · lint ✅ · build 28.8s ✅ · 3 فاحصات ✅ (بخط أساس).
- **المشكلة المقاسة:** الـ 24 صفحة خدمة×مدينة **متكررة 97%** (صبابين-أبها ↔ صبابين-الطائف = 96.93%). 118 زوجاً فوق عتبة 60%. هذا doorway عند Google. مسجّل كـ «خط أساس» ولم يُصلَح.
- **المشكلة الثانية (شكوى العميل):** الرئيسية بلا صورة أعمال واحدة، الفوتر 41% من الارتفاع، 3 أزرار واتساب متزاحمة.
- **ما هو قوي ويُحفظ:** Schema.org كامل (CateringService SAB + Service + Breadcrumb + WebPage + ImageGallery) · 3 sitemaps بـ lastmod صادق · middleware (www→non-www + روابط WordPress) · CSP/HSTS · next/image في كل مكان · فاحصات سيو/تشابه/أصول · `data/proof.json`.
- **الإنتاج متأخر:** keifaldiafa.com لا يعرف آخر 3 commits (`/mubashirin-qahwa-jeddah` = 404 حياً).
- **CI مفعّل 2026-09-09** (`.github/workflows/quality.yml` = `ci/quality.yml`، دُفع بتوكن المالك ذي صلاحية workflow — التوكن ليس محفوظاً في أي ملف). أول تشغيل: quality ✅ · lighthouse ❌ (continue-on-error، مراقبة فقط — سقف desktop 0.9 لا يتحقق قبل المرحلة 3).

### 2.2 النموذج (allpro/prototype-home v6.9)
- 43 صفحة بنفس مسارات Next + صفحة **`/links`** جديدة (باركود موحّد، 9 بطاقات، تأثير الأومنتريكس).
- كل صفحة فرعية = **صفحة كاملة بمستوى الرئيسية** (16 قسماً: هيرو + لمن نخدم + أعمالنا + شركاء + الأدوار + الزي + التقديمات + الخدمات + لماذا نحن + الأسئلة + الأحياء + الروابط + تواصل + فوتر).
- خطوط محلية مُقلَّصة: `amiri-700.woff2` (العناوين) · `noto-naskh.woff2` (النص) · `marcellus.woff2` (اللاتيني). لوحة: فحمي `#1A1A1A` / ذهبي `#C5A059` / كريمي `#F5F5F5`.
- اختُبر: Lighthouse جوال 93–99 · a11y 100 · 0 أخطاء على 390 و1440.

### 2.3 كتالوج الصور (v4 — 2026-09-07)
| البند | العدد |
|---|---|
| السجلات | **325** |
| يُنشر = نعم | **274** |
| زخرفي فقط | 40 (خلفيات — لا image-sitemap ولا ImageObject) |
| لا يُنشر | 11 (غير موجودة في `web/` أصلاً) |
| المستويات | 1: 18 · 2: 76 · 3: 71 · 4: 92 · 5: 18 · 6: 50 |
| شعارات الشركاء | 49 (يُعرض: 46 · بعد المراجعة: 3) |

**قاعدة الترتيب:** `tier` تصاعدياً ثم `رقم`. **المستوى 1 (حكومي/جامعات/غرف تجارية) أولاً دائماً** في الرئيسية و`/portfolio`.

---

## 3. القرارات الحاكمة (سارية — تلغي أي قرار أقدم)

| # | القرار | المصدر |
|---|---|---|
| D109 | **إلغاء P4.** أسماء الوزارات/الجامعات/الأمانات **تُذكر وتُعرض أولاً** — أقوى إثبات. `photo-vetting.json` يُتجاهل كلياً. | رسالة المالك 2026-09-06 |
| D111 | الوكيل المبرمج **لا يحلل صوراً ولا يغيّر اسماً أو alt** — يأخذ من `catalog.json` كما هو. | تقرير 33 |
| D113 | **إلغاء العلامة المائية البرمجية.** الصور في `web/` مدموجة بالشعار أصلاً. لا معالجة في الكود. لا `WATERMARK.md`. | كلام المالك 2026-09-07 |
| D114 | لا مستودع جديد. العمل في `keif-aldiafa-web/main` بعد فرع احتياطي. | جلسة 2026-09-07 |
| D115 | الهيكلية = **نفس هيكلية prototype-home بلا تغيير**، تُبنى بـ Next بأعلى المعايير، «بلا ترقيع من البداية». | صوت المالك 2026-09-07 |
| D116 | **الترتيب:** الهيكلية كاملة أولاً (المراحل 1–6) ← ثم الصفحات الفرعية الجديدة (المرحلة 7). لا خلط. | صوت المالك 2026-09-07 |
| D117 | الفحص على **4 مقاسات**: 390 (جوال) · 768 (تابلت) · 1024 · 1440. | صوت المالك: «متلائم على التابلت وجميع الأجهزة» |
| — | FAQPage JSON-LD محذوف (Google أوقفتها 2026-05-07). priceRange `$$-$$$$` يبقى. | تقارير 13/14 |

### القواعد الثابتة (لا تُخالَف)
1. **رقم واحد:** `0508252134` عرضاً · `wa.me/966508252134` رابطاً. مصدره `src/lib/site.ts`.
2. **الأرقام المسموحة فقط:** +500 مناسبة · منذ 2016 · س.ت 7033069720. أي رقم آخر يحتاج `verified_by_owner:true` في `data/proof.json`.
3. **لا أسعار** في أي صفحة. **لا يوتيوب** (المالك: «ما عندي»). لا LinkedIn/Telegram.
4. **لا أقواس حول أرقام في نص عربي** (تنقلب bidi) — استخدم «·».
5. **لا تغيير slug ولا حذف صفحة** — الروابط مفهرسة في Google. مدن Next بالعربي (`/locations/جدة`) تبقى كما هي.
6. **لا Pull Requests** — commit + push مباشر إلى `main` بعد بوابة الجودة. قبل أي دمج كبير: فرع `old-version-YYYY-MM-DD`.
7. **لا نشر إلى keif-v2/Vercel** إلا بأمر صريح من المالك.
8. كل قرار جديد يُرقَّم **D118 فصاعداً** ويُكتب في `docs/DECISIONS.md`.
9. كل رسالة من المالك تُحفظ حرفياً في `docs/owner-messages/YYYY-MM-DD-<موضوع>.md` قبل التنفيذ.
10. **لا كتابة API/دالة لم تُقرأ فعلياً** في هذه الجلسة (node_modules / .d.ts / التوثيق الرسمي).

### بوابة الجودة (كل مرحلة تخرج بها خضراء)
```
npx tsc --noEmit  &&  npm run lint  &&  npm run build  &&  npm run guard
+ Playwright: 390/768/1024/1440 → 0 أخطاء console · 0 تمرير أفقي · 0 صورة مكسورة
+ Lighthouse جوال: Performance ≥90 · Accessibility 100 · Best Practices ≥90 · SEO 100
+ لقطات قبل/بعد في docs/shots/<المرحلة>/
```

---

## 4. الخطة — المراحل

> الرمز: ⬜ لم تبدأ · ⏳ جارية · ✅ منجزة (منجزة = مُنفَّذة + مُختبَرة + مُلتزَمة في main)

### ✅ المرحلة 0 — التثبيت (صفر تغيير في الموقع)
- [x] كتابة `PLAN.md` (هذا الملف)
- [x] `docs/DECISIONS.md` بالقرارات D109–D117
- [x] حفظ `PROMPT-AGENT-BOOTSTRAP-v2.txt` في `docs/`
- [x] فرع احتياطي `old-version-2026-09-07` مرفوع
- [ ] ⚠️ تفعيل CI — **يدوي من المالك**: توكن الوكيل بلا صلاحية `workflows`. الخطوة: في GitHub → أنشئ `.github/workflows/quality.yml` بنسخ محتوى `ci/quality.yml` (أو أعطِ التوكن صلاحية workflows)
- [x] commit + push إلى main
- **بوابة الخروج:** الملفات موجودة، الفرع الاحتياطي مرفوع، tsc/lint/build خضراء.

### ✅ المرحلة 1 — الصور والشعارات (البيانات قبل الشكل) — 2026-09-08
- [x] `scripts/import-catalog.mjs` يقرأ `catalog.json` + `partners.json` من استنساخ محلي (`/tmp/catalog`) ويولّد:
  - [x] `src/lib/imageCatalog.data.ts` — 314 سجلاً `{ id, tier, file, width, height, alt, title, entity, sector, pages[], publish }` (أبعاد `web_*` مُتحقَّقة بـ `identify` 314/314)
  - [x] `src/lib/partners.ts` — 49 شعاراً بالأولوية ثم الرقم · `PARTNERS` = 46 يُعرض
- [x] `web/**` (314) → `public/images/catalog/` (مسطّح، 29MB) · `logos/web/*` (49) → `public/images/partners/` (680KB) — 67 شعاراً قديماً حُذف
- [x] `src/lib/imageCatalog.ts` أُعيدت كتابته على المصدر المولَّد **بنفس الواجهة** (D118) — sitemap/ImageGallery انتقلت تلقائياً · `PartnersMarquee` على `partners.ts`
- [x] `image-sitemap` = **274** صورة (publish=نعم فقط) تحت 11 صفحة · `ImageObject` بأبعاد حقيقية · الزخرفي (40) مستثنى
- [x] `locations/[city]` + `localContent` (24 صفحة): صور الكتالوج المستهدفة للمدينة أولاً + alt حرفي (D111) + إزالة العلامة البرمجية (D113)
- [x] حذف 10 صور قديمة لا يشير إليها كود · 349 باقية لأن الصفحات القديمة تستهلكها (تُحذف مع المراحل 3–5)
- [x] فاحص الأصول: **CH9** (alt الكتالوج حرفياً في HTML المبني، مُختبَر سلباً) · خط الأساس 31 → **27**
- **بوابة الخروج (مقاسة):** tsc ✅ · lint ✅ · build ✅ · guards ✅ (seo 118 · sim 118 · assets 27) · زحف 42 صفحة = **0 صورة مكسورة** من 315 مرجعاً · Playwright 4 مقاسات × 4 صفحات = 16/16 (0 console · 0 تمرير أفقي · 0 مكسورة) · لقطات `docs/shots/phase-1/`
- **لم أفحصه:** Lighthouse (يُقاس في المرحلة 3 حين يتغيّر الشكل فعلاً).

### ✅ المرحلة 2 — الشِل (ما يظهر في كل صفحة) — 2026-09-09
- [x] الخطوط: 3 woff2 من النموذج → `public/fonts/` (amiri-700 43KB · noto-naskh 22KB · marcellus 9KB) + `next/font/local` في `layout.tsx` · **حُذف** Tajawal/Cairo/El Messiri (Google Fonts) — 0 طلبات إلى fonts.googleapis
- [x] `src/styles/v7.css` = CSS الشِل من النموذج منقول 1:1 (المتغيرات · الأساس · عناوين الأقسام · `.rv` · الهيدر · الأزرار · الفوتر · `.fab` · `.lb`) — اختلافان موثقان: بلا `@font-face` (next/font) وبلا شريط `.proto`
- [x] `src/components/v7/`: `Header` (صفّان على الجوال 107px / صف واحد 65px من 900) · `Footer` (خادم صرف: علامة+تواصل / خدمات / 8 مدن / doormat / 3 شهادات / الرقم الموحّد) · `WhatsAppFab` (واحد، يختفي في `.hero` و`#contact`) · `Lightbox` (data-g، تفويض أحداث، لوحة مفاتيح، سحب) · `Reveal` (`.rv`, reduced-motion)
- [x] `ClientLayout` على الشِل الجديد · **حُذف** `Navbar.tsx` `Footer.tsx` `FloatingWhatsApp.tsx` القديمة (وطلب PWA معها)
- [x] `check-assets` CH5: استثناء موثق لـ `v7/Lightbox` (يعرض src جاهزاً من DOM)
- **بوابة الخروج (مقاسة):** tsc ✅ · lint ✅ · build ✅ · guards ✅ (seo 118 · sim 118 · assets 27) · Playwright 4 مقاسات × 6 صفحات = **24/24** (0 console · 0 مكسورة · 0 تمرير أفقي · هيدر+فوتر جديدان · fab=1 في كل صفحة) · axe على الشِل = **0 مخالفات** WCAG 2.1 AA · اختبار Lightbox e2e (فتح بالفهرس الصحيح · أسهم + التفاف · Esc · استعادة التركيز · Enter · ×) ✅ · Lighthouse جوال الرئيسية: **A11y 100 · SEO 100** · Perf 40 / BP 77 (جسم الرئيسية القديم — المرحلة 3) · لقطات `docs/shots/phase-2/`
- **ملاحظة:** الصفحات الداخلية ما زالت بجسمها القديم (Tailwind/luxe) تحت الشِل الجديد — متوقَّع حتى المراحل 3–5.

### ✅ المرحلة 3 — الصفحة الرئيسية — 2026-09-09 (بانتظار اعتماد المالك)
- [x] `components/home/HomePage.tsx` (خادم) = أقسام النموذج بالترتيب: هيرو (`<picture>` عمودية/عريضة + فيديو) → لمن نخدم (3) → أعمالنا (شريط 12 · المستوى 1 كاملاً) → شركاء النجاح (كريمي · 46 شعاراً بالأولوية · +500 · «عرض الكل» على الجوال) → الطاقم والزي ×8 → التقديمات ×13 → الخدمات 12 في 3 مجموعات → الأدوار → الترتيبات → لماذا نحن → المدن → الأسئلة → تابعنا → تواصل
- [x] «use client» فقط في `Strip` (حلقة لا نهائية + سحب) · `HeroVideo` · `PartnersMore` (+ Lightbox/Reveal من الشِل) — D124
- [x] الصور من الكتالوج فقط عبر `CatalogImg` (next/image · alt/data-cap حرفيان — CH9 يفحص 124 صورة) · بيانات الصفحة `lib/homeContent.ts` · **حُذف** `HomePageClient.tsx` `PartnersMarquee.tsx` `homeFaqs.ts`
- [x] CSS أقسام الرئيسية من النموذج 1:1 في `v7.css` (+ قواعد كان يحقنها JS النموذج) · انحرافان مقاسان لـWCAG (D125) · `content-visibility:auto` تحت الطية (D124)
- **بوابة الخروج (مقاسة):** tsc ✅ · lint ✅ · build ✅ (الرئيسية 1.95KB · 103KB أول تحميل) · guards ✅ (seo 118 · sim 118 · assets 27) · Playwright 4 مقاسات = **4/4** (0 console · 0 مكسورة · 0 تمرير أفقي · 3 أشرطة تعمل · Lightbox يفتح بالفهرس الصحيح 3/12 · رابط الخدمة يفتح المعرض بلا تنقّل · زر الشركاء 12→47) · axe كامل الصفحة 390+1440 = **0** · Lighthouse جوال: **A11y 100 · SEO 100** · Perf **72** (من 40 — الهدف ≥90 لم يتحقق: المتبقي JS مشترك + gtag، دَين للمرحلة 6) · BP 77 (gtag cookies) · لقطات `docs/shots/phase-3/` (قبل = `phase-2/`)
- [ ] **اعتماد المالك** للرئيسية (390 + 1440).

### ✅ المرحلة 4 — قالب الصفحة الفرعية الكاملة (D127–D131 · Playwright 16/16 على 390/768/1024/1440 · axe 0 · Lighthouse mobile `/qahwajiin-jeddah` 69/100/77/100 بعد إحماء محسّن الصور، 51 على أول طلب بارد · لقطات `docs/shots/phase-4/`)
- `LocalServicePage` بخاصية `kind: 'svc' | 'city' | 'intent'` وفق `master_page()` في النموذج (16 قسماً)
- الصفحة النموذجية: **`/qahwajiin-jeddah`** (أعلى نية: 1,642 نقرة مدفوعة) → اعتماد المالك → تُطبَّق على 24 + 8 + 1
- دمج نصوص `local.py` مع `localContent.tsx`: الهيكل من النموذج، **النص الأغنى يفوز**، كل اختيار يُوثَّق
- **بوابة الخروج:** 33 صفحة خضراء · فاحص التشابه لا يرتفع فوق خط الأساس (تصفية التكرار = المرحلة 7).

### ✅ المرحلة 5 — الصفحات العامة + /links — 2026-09-09 (D132–D138 · `/links` أُكملت في المرحلة 6 D145)
- `/services` (15 خدمة من `data.py`) · `/offerings` (76 صنفاً) · `/portfolio` (80 صورة، المستوى 1 أولاً، فلتر جهات) · `/about` · `/contact` (9 بطاقات + الأومنتريكس + نموذج واتساب) · `/legal` · `/social` · `/locations`
- **`/links` جديدة** (مسار جديد مسموح — إضافة لا تغيير): شعار + 9 بطاقات فقط، بلا هيدر/فوتر، `noindex`؟ **لا** — تُفهرس (هي وجهة الباركود المطبوع)
- تحديث `sitemap.ts` (+`/links`، lastmod صادق)
- **بوابة الخروج (مقاسة):** 43 مساراً في sitemap بالشكل الجديد (42 + `/links`) · 3 sitemaps = 200 · HTML audit 43/43.

### ✅ المرحلة 6 — الأداء والفحص الشامل — 2026-09-09 (D140–D146 · `docs/reports/2026-09-09-phase-6-performance.md`) · ⬜ **النشر ينتظر أمر المالك**
- Playwright 45 صفحة × 4 مقاسات · Lighthouse على 8 صفحات ممثلة · فحص HTML الناتج (canonical/robots/JSON-LD)
- تقرير قبل/بعد بالأرقام في `docs/reports/`
- تحديث `PLAN.md` + `docs/DECISIONS.md`
- **ثم فقط:** طلب أمر النشر من المالك → نسخ إلى `keif-v2` → Vercel
- **بوابة الخروج:** أمر المالك الصريح — **لم يصدر بعد**.

### ⬜ المرحلة 7 — الصفحات الفرعية الجديدة (بعد اكتمال 1–6 فقط)
- استخلاص الكلمات من `allpro/01-keif-aldiafa/data/ads-processed/` (Ads + GSC) + تقرير 11 → قائمة تُعرض على المالك للاعتماد
- الطبقة 1 — **النوايا** (فرق حقيقي في الخدمة): قهوجيين · صبابين · مباشرين · قهوجيات · صبابات · مباشرات · ضيافة أعراس · كوفي بريك شركات · ضيافة عزاء … كل نية = نص يدوي فريد (كما `/mubashirin-qahwa-jeddah` = تشابه 36–40%)
- الطبقة 2 — **المدن**: تنويع ببيانات حقيقية (صور بدليل مكاني من الكتالوج، أحياء وقاعات، أسئلة مختلفة)
- صفحات يشير إليها الكتالوج فعلاً (جاهزة الصور): `/coffee-break-sharikat-jeddah` (87 صورة) · `/diyafa-a3ras-jeddah` (36) · `/qahwajiyat-sababat-jeddah` (16)
- **الحارس:** فاحص التشابه يُشدَّد إلى **≤45%** لأي صفحة جديدة · معالجة الـ 24 صفحة المتكررة (97%) بإعادة كتابة أو دمج — **قرار المالك عند الوصول**
- **بوابة الخروج:** الهدف 50–100 صفحة، كل زوج ≤45%.

---

## 5. أين توقفنا · ماذا بعد

| | |
|---|---|
| **آخر ما أُنجز** | المرحلة 6 كاملة: gtag مؤجّل (BP 100) · كتالوج خارج حزمة العميل · `.eq` · 333 صورة قديمة محذوفة · `/links` (D145) · فحص شامل 168/168 + axe 0 + HTML 43/43 · D140–D146 |
| **الجاري** | لا شيء — البناء والتدقيق والتوثيق مكتملة |
| **التالي فوراً** | انتظار المالك: (1) طريقة مراجعة الصور (D139) (2) قرار مقاسات `.tag/.l2` (D138) (3) أمر النشر. التفصيل والترتيب في `docs/AGENT-STATE.md` §3 |
| **ينتظر المالك** | مراجعة الصور بطاقة‑بطاقة · تفعيل CI يدوياً · حذف التوكن القديم من GitHub · أمر النشر |

## 6. كيف تبدأ جلسة جديدة (للمالك)

الصق هذا السطر فقط:

> **اقرأ `PLAN.md` و`docs/DECISIONS.md` في مستودع `MoTechSys/keif-aldiafa-web` كاملَين، ثم `docs/PROMPT-AGENT-BOOTSTRAP-v2.txt`، وأعطني تقرير الجاهزية، ثم كمّل من «أين توقفنا» بلا أي سؤال إلا عند الضرورة.**

## 7. أوامر الجلسة (للوكيل)

```bash
# استنساخ المصادر (للقراءة) — الكتالوج لازم لإعادة توليد imageCatalog.data.ts
git clone --depth 1 --filter=blob:none --sparse https://github.com/MoTechSys/allpro /tmp/allpro && cd /tmp/allpro && git sparse-checkout set prototype-home 01-keif-aldiafa/data
git clone --depth 1 https://github.com/MoTechSys/catalog-keif-aldiafa-photots /tmp/catalog

# إعادة استيراد الكتالوج عند تحديثه (المرحلة 1)
node scripts/import-catalog.mjs /tmp/catalog

# بوابة الجودة
cd /home/user/webapp && npx tsc --noEmit && npm run lint && npm run build && npm run guard

# الالتزام (لا PR)
git add -A && git commit -m "<type>(<scope>): <وصف عربي واضح>" && git push origin main
```

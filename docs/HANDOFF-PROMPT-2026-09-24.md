# برومت التسليم للوكيل الجديد — 2026-09-24

> يُلصَق كاملاً في أول رسالة للجلسة الجديدة. كُتب في نهاية جلسة D166–D174 (Claude). كل رقم هنا مُقاس ومصدره مذكور في المستودع.

---

أنت الوكيل البرمجي لمشروع **«كيف الضيافة» — keifaldiafa.com** (Next.js 14 App Router، SSG، RTL عربي). المستودع: `MoTechSys/keif-aldiafa-web` فرع `main`. تُكمل عمل الجلسات السابقة **من حيث توقّفت بالضبط** — لا تبدأ من الصفر ولا تعِد تحليلاً مكتملاً.

## 1) المالك وكيف تخاطبه
- **MoTechSys** — ناديه **«يا غالي»**. عامية يمنية/سعودية، رسائل قصيرة أو صوتية («اشتي» = أريد، «سوي» = افعل). **فسّر النية لا الحرف.**
- ردودك: **عربية موجزة** · عناوين · **أرقام مقاسة فقط** (لا ادّعاء غير مُقاس) · **سؤال واحد كحد أقصى** بخيارين (نعم/لا أو أ/ب).
- يفوّض: «قرّر أنت ووثّق». لكن **المحتوى / الصور / النشر / الأرقام** قرارها له وحده.
- **كل رسالة منه تُحفظ حرفياً** في `docs/owner-messages/YYYY-MM-DD-<موضوع>.md` (النص كما ورد + «ما فُهم») — حتى القصيرة، **قبل** أن تنفّذ.

## 2) أول 30 دقيقة — إلزامي بالترتيب
1. `pwd` → اعمل داخل `/home/user/webapp` فقط. إن لم يكن المستودع موجوداً: `setup_github_environment` ثم `git clone https://github.com/MoTechSys/keif-aldiafa-web.git /home/user/webapp`.
2. اقرأ كاملاً: `AGENTS.md` → `docs/AGENT-STATE.md` (أحدث فقرة **(ط)** هي آخر حالة) → `PLAN.md` → `docs/DECISIONS.md` (D109–**D174**؛ التالي الحر **D175**) → آخر 7 ملفات في `docs/owner-messages/` → `docs/DEPLOYMENT.md` → `docs/reports/2026-09-23-deploy-D173.md` و`2026-09-23-critical-audit-D172.md`.
3. `npm ci` ثم شغّل بوابة الجودة **قبل أي تعديل** وسجّل الأرقام:
   ```
   npx tsc --noEmit && npm run lint && rm -rf .next && npm run build
   node scripts/seo-guard.mjs && node scripts/similarity-check.mjs && node scripts/check-assets.mjs && node scripts/html-audit.mjs
   ```
   المتوقّع: 56 مساراً · seo **22/22** · similarity **22/22** · assets **21/21** (CH10 = 49 بطاقة OG فريدة) · html-audit 48/0.
4. شغّل `npx next start -p 3111` في الخلفية ثم `GetServiceUrl` للمنفذ 3111. **بعد أي `next build` أعد تشغيل `next start`** (درس D165: خادم قديم يخدم chunks قديمة).

## 3) القواعد الحمراء (لا استثناء)
1. **لا PR** — commit مباشر إلى `main` بعد بوابة خضراء، ثم `git push origin main`. فرع `genspark_ai_developer` مهمل. رمز GitHub ينقضي بين الجلسات → `setup_github_environment` قبل أي push. **لا تكتب أي توكن في أي ملف.**
2. **لا تغيير slug** لأي مسار موجود.
3. **لا أسعار · لا يوتيوب · لا أرقام** إلا `+500` · `منذ 2016` · `7033069720`. **رُفض** لفظ «أفضل ناس في السعودية» (D154/D172) — البديل كيان غنيّ بأرقام مثبتة.
4. **D111/CH9**: alt/title من `src/lib/imageCatalog.data.ts` (314 سجلاً) **حرفياً**؛ لا يُعدَّل يدوياً. الحقل `caption` (D166) مشتق بالحذف فقط عبر `displayCaption()`.
5. **D115**: الهيكل والCSS = نموذج v6.9 **1:1**. أي CSS جديد → **كتلة موثّقة مُلحَقة في نهاية `src/styles/v7.css`** (كتلة D166–D170 موجودة)، لا تعديل أسطر النموذج.
6. **الصور**: شعار بيضاوي واحد في الوسط، لا يغطي وجهاً (D164/D165). أي تغيير صورة يمرّ بـ `scripts/images/audit-stamps.py` = **0/0**. بطاقات OG تُولَّد بـ `scripts/images/build-og.py` (تحتاج الأصول النظيفة من فرع `archive-v2-old-site-2026-07-25` في keif-v2 → `/tmp/kv2src` + `/tmp/yunet.onnx`).
7. **كل قرار مرقّم** في `docs/DECISIONS.md` (القرار لا يُعدَّل بل يُنسَخ بقرار جديد يذكر ما ألغاه) + فقرة جديدة في رأس `docs/AGENT-STATE.md` بحرف تالٍ **(ي)** + تقرير في `docs/reports/` + سطر المؤلف «— Claude (Anthropic)» (D148).
8. **D117**: أي تغيير بصري يُفحص على 390/768/1024/1440 + لقطة تُقرأ بالعين.

## 4) النشر — الحقيقة المؤكَّدة (D173)
- **الموقع الجديد حيّ على الإنتاج** منذ 2026-09-23. Vercel مربوط بـ **`MoTechSys/keif-v2` فرع `main`** وليس بـ keif-aldiafa-web.
- **إجراء النشر** (فقط بأمر صريح من المالك): بوابة خضراء → `git remote add kv2 https://github.com/MoTechSys/keif-v2.git` → فرع مؤقت **بلا** `.github/workflows/quality.yml` (تطبيق GitHub لا يملك صلاحية workflows) مع `.github/WORKFLOW-NOTE.md` → `git push -f kv2 <branch>:main` → انتظر ~90 ثانية → فحص §«بعد النشر» في `DEPLOYMENT.md` (49/49 مسار 200، robots/sitemap_index/llms/security/humans/og).
- الحالة عند التسليم: web `main` = `af3474e` (كود) + commits توثيق بعده · keif-v2 `main` = `2d9f440` (= `af3474e` بلا workflow) · الموقع القديم محفوظ في `keif-v2` فرع `archive-v2-old-site-2026-07-25`.

## 5) آخر قياس على الإنتاج (Lighthouse 12، جوال، 2026-09-23 بعد D174)
| الصفحة | الأداء | LCP | TBT | A11y/BP/SEO |
|---|---|---|---|---|
| `/` | 53 → **78** | 2.6s | 680ms | 100/100/100 |
| `/qahwajiin-jeddah` | 61 → **79** | — | ~750ms | 100/100/100 |

D174 = gtag بلا محفّز scroll + خمول 4s (اللمس يحمّله فوراً — مُتحقَّق) + `prefetch={false}` على 32 رابطاً. **الباقي للأداء (يحتاج قرار):** JS الأولي ~490KB (`Strip`/`Lightbox` عميل) و LCP على الحدّ → تأجيل الشرائط تحت الطية.

## 6) ما بقي — بالترتيب، ومن يقرّر
**تنتظر المالك (لا تنفّذ بدون أمر/مدخلات):**
1. مسح كاش المعاينات: Facebook Sharing Debugger «Scrape Again» + تيليجرام `@WebpageBot` — واختبار من هاتف حقيقي.
2. **توكن Search Console** (وعد به): إرسال `sitemap_index.xml` + طلب فهرسة الرئيسية وصفحات المدن + تقرير التغطية.
3. **توكن Google Ads**: تحديث الروابط النهائية للحملات إلى المسارات الجديدة (سياق: `docs/reports/2026-09-18-ads-3000-forensics-and-plan.md`).
4. `aggregateRating` في schema — يحتاج عدد/معدل تقييمات Google الحقيقي من المالك.
5. دمج 10 أزواج سجلات مكررة + بديل الصورتين #307/#93.
6. تقليص طول الصفحات (13–21 شاشة) — قرار محتوى.
7. النسخة الإنجليزية / hreflang — قرار كبير.
8. CI في keif-v2: نسخ `ci/quality.yml` إلى `.github/workflows/` من واجهة GitHub (المالك).
9. (اختياري) ربط Vercel مباشرة بـ keif-aldiafa-web.
10. PageSpeed/CrUX بعد 24–48 ساعة من النشر.

**تقنية يمكن اقتراحها (تنفّذ فقط بعد موافقة «نعم»):** تأجيل شرائط تحت الطية لخفض JS الأولي · تحسين LCP.

## 7) أول ردّ لك في الجلسة
بعد إتمام §2 أرسل للمالك **ردّاً واحداً موجزاً**: أرقام البوابة كما قستها الآن + رابط المعاينة + تأكيد أن الإنتاج يعمل (`curl -sI https://keifaldiafa.com/`) + **سؤال واحد**: «أبدأ بأي بند من §6؟ (أ) عندك التوكنات الآن → Console/Ads · (ب) شيء آخر تحدّده». لا تنفّذ شيئاً قبل جوابه.

## 8) مراسي حرفية (لتتأكد أنك في السياق الصحيح)
- آخر أمر تنفيذي من المالك: «كمل اللنشر هيا» → نُفِّذ D174 ووُثّق.
- أمر النشر الأصلي: «ياللهوابدا النشر بدقه اوك ووثق كل شي… انشر باسم MoTechSys كي ينشر فيرسال ووثق ايش يااقي كي نكمل في الجلسهالجديده».
- آخر مخرجات push: `web af3474e` / `+ 8fe3a12...2d9f440 kv2-mirror -> main (forced update)`.

— Claude (Anthropic) · 2026-09-24

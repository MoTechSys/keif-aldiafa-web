# رسالة المالك — 2026-09-11 — توكن GitHub للدمج والاتصال بالمستودع

> محفوظة حرفياً كما وردت — **التوكن نفسه محجوب عمداً** (لا يُكتب سر في المستودع أبداً — BOOTSTRAP §6.3).

# Account: MoTechSys
ghp_******************************** (محجوب)

طبعا استخدم التوكن التي
للدمج والتصال با الستودع اوك

## الفهم التنفيذي
- استخدام توكن المالك (PAT) للمصادقة على `git push origin main` بدل توكن الوكيل الذي يسقط أحياناً.
- التوكن يُخزَّن في `~/.git-credentials` خارج المستودع فقط؛ لا يُطبع ولا يُلتزم ولا يُسجَّل.
- **تنبيه للمالك**: التوكن كُشف في نص المحادثة → يُنصح بحذفه من GitHub → Settings → Developer settings → Tokens بعد نهاية الجلسة (كما البند A5 في PLAN §4½).

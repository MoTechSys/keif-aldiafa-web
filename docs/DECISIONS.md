# DECISIONS.md — سجل القرارات المرقّمة (keif-aldiafa-web)

> قرار واحد لكل صف. القرارات **لا تُعدَّل** — تُنسَخ بقرار جديد يذكر ما ألغاه. التالي الحر: **D163**.
> القرارات D1–D108 موثقة في `MoTechSys/allpro/02-context-memory/STATE.md` (تاريخية).

| # | التاريخ | القرار | يلغي | المصدر |
|---|---|---|---|---|
| D109 | 2026-09-06 | إلغاء P4 (حظر أسماء الجهات الحكومية). الوزارات/الجامعات/الأمانات **تُذكر وتُعرض أولاً** — أقوى إثبات. `photo-vetting.json` يُتجاهل. | P4 · تقارير 03/08 | رسالة المالك الصوتية: «متى أنا قلت ممنوع؟» — `catalog/docs/2026-09-06-owner-decision-*.md` |
| D110 | 2026-09-06 | ترتيب القوة 6 مستويات: حكومي/جامعات ← شركات كبرى ← طاقم وقاعات ← بوفيهات/معدات ← فنية ← عام/ستوك. | — | نفس المصدر |
| D111 | 2026-09-06 | التسمية = المحتوى الفعلي للصورة. الوكيل المبرمج **لا يحلل ولا يسمّي** — يأخذ `catalog.json` كما هو. | — | نفس المصدر |
| D112 | 2026-09-06 | فصل الأدوار: وكيل التحليل يملك الصور/alt؛ وكيل البرمجة يُدرج فقط. | — | نفس المصدر |
| D113 | 2026-09-07 | **إلغاء العلامة المائية البرمجية.** الصور في `catalog/web/` مدموجة بالشعار أصلاً. لا معالجة في الكود، لا `WATERMARK.md`. | WATERMARK v5 · تقرير 33 §5.7 | صوت المالك 2026-09-07 |
| D114 | 2026-09-07 | **لا مستودع رابع.** العمل في `keif-aldiafa-web/main` بعد فرع `old-version-2026-09-07`. | — | سؤال المالك الصوتي + توصية الوكيل (حفظ الفاحصات/Schema/التاريخ) |
| D115 | 2026-09-07 | الهيكلية = **نفس هيكلية `allpro/prototype-home` v6.9 بلا تغيير**، تُبنى بـ Next.js بأعلى المعايير «بلا ترقيع من البداية». | — | صوت المالك 2026-09-07 |
| D116 | 2026-09-07 | **الترتيب:** الهيكلية كاملة (المراحل 1–6) ← ثم الصفحات الفرعية الجديدة (المرحلة 7). لا خلط. | — | صوت المالك 2026-09-07 |
| D117 | 2026-09-07 | الفحص على **4 مقاسات**: 390 · 768 · 1024 · 1440. | P7 (390+1440 فقط) | صوت المالك: «متلائم على التابلت وجميع الأجهزة» |
| D118 | 2026-09-08 | **مصدر الصور = ملف مولَّد** `src/lib/imageCatalog.data.ts` من `catalog.json` عبر `scripts/import-catalog.mjs` (لا مسح مجلد، لا اشتقاق alt من اسم الملف). واجهة `imageCatalog.ts` العامة محفوظة فتنتقل الخرائط والـ schema تلقائياً. الصور في `public/images/catalog/` مسطّحة، الشعارات في `public/images/partners/`. | مسح `public/images/**` القديم | المرحلة 1 — PLAN §4 |
| D119 | 2026-09-08 | **صور صفحات المرحلة 7 تُفهرَس من اليوم**: صورة يستهدف الكتالوج بها صفحة لم تُبنَ بعدُ (مثل `/coffee-break-sharikat-jeddah`) تؤوى في `/portfolio` بالخريطة وتُنقل تلقائياً حين تُبنى الصفحة. | — | قرار الوكيل — الفهرسة المبكرة أهم من الانتظار |
| D120 | 2026-09-08 | **الصور القديمة تبقى حتى المرحلة التي تستبدلها**: حُذف فقط ما لا يشير إليه أي كود (10 ملفات). الـ 349 الباقية (services/offerings/keif/cutouts…) تُحذف مع إعادة بناء صفحاتها (المراحل 3–5). `showWatermark` أُزيل من صور الكتالوج فقط (D113)؛ إزالته الكاملة من `ProtectedImage` = المرحلة 2. | — | تجنّب كسر صفحات لم تُعاد كتابتها بعد |
| D121 | 2026-09-09 | **الشِل من النموذج 1:1** (`src/styles/v7.css` = مقاطع الشِل من `<style>` النموذج بلا تعديل؛ الخطوط الثلاثة عبر `next/font/local` بمتغيرات `--font-amiri/--font-naskh/--font-marcellus`). عناصر الشِل مُسمّاة `header.v7-header` و`footer.v7-footer` كي لا تصطدم بصفحات لم تُعاد كتابتها (المراحل 3–5). Tajawal/Cairo/El Messiri من Google Fonts **حُذفت**؛ متغيراتها القديمة تُوجَّه مؤقتاً إلى الخطوط الجديدة (جسر يُزال في المرحلة 5). | Navbar/Footer/FloatingWhatsApp القديمة · Google Fonts | المرحلة 2 — D115 |
| D122 | 2026-09-09 | **المعرض الموحّد `Lightbox`** يعمل بتفويض الأحداث على `document` (لا مستمع لكل figure كما في النموذج) — يخدم العناصر المستنسخة/المضافة بعد التحميل وتنقّل App Router بلا إعادة ربط. العقد: `figure[data-g]` + `img[data-cap][data-sub]` + `figure[data-go][data-go-txt]`. لا استبدال مسار للصورة الكاملة (صور الكتالوج هي النسخة الكاملة — D113). | — | المرحلة 2 |
| D123 | 2026-09-09 | **واتساب طافٍ واحد** (`WhatsAppFab`) في الموقع كله، يختفي حين يظهر `.hero` أو `#contact` (عتبات النموذج .15/.2). زر «تواصل الآن» في الهيدر + زر الفوتر يبقيان (كما النموذج). طلب تثبيت PWA الذي كان في Navbar القديم **أُزيل** — ليس في النموذج. | 3 أزرار واتساب متزاحمة (شكوى المالك) | المرحلة 2 — D29 |
| D124 | 2026-09-09 | **الرئيسية = مكوّن خادم** `components/home/HomePage.tsx` (أقسام النموذج v6.9 بالترتيب 1:1، النصوص حرفية) و«use client» محصور في `Strip` (حلقة الشريط) و`HeroVideo` و`PartnersMore`. فرقان أداء مقصودان عن JS النموذج: (أ) الاستنساخ وحلقة rAF يبدآن بعد أول خمول ولا يقرآن التخطيط كل إطار؛ (ب) `content-visibility:auto` على الأقسام تحت الطية (عدا الهيرو). بيانات الصفحة في `lib/homeContent.ts` بأرقام الكتالوج؛ شريط «أعمالنا» = المستوى 1 كاملاً (12) بعناوين الكتالوج بدل صور/تعليقات النموذج القديمة. | `HomePageClient.tsx` · `PartnersMarquee.tsx` · `homeFaqs.ts` (حُذفت) | المرحلة 3 — PLAN §2.3 «المستوى 1 أولاً» |
| D125 | 2026-09-09 | **انحرافان بصريان مقاسان عن CSS النموذج** بسبب WCAG AA (axe/Lighthouse A11y 100): `.partners .kinds` من `--gold-bz` (3.46:1 على الكريمي) إلى `--gold-deep` من نفس اللوحة (5.3:1)؛ ونسخ الأشرطة المستنسخة (`data-clone`) تحمل `tabindex=-1` لأنها `aria-hidden`. لا تغيير آخر في القيم. | — | axe-core WCAG 2.1 AA = 0 |
| D126 | 2026-09-09 | **هيرو `<picture>` بتوجيه فني** (عمودية #106 على الجوال · عريضة #99 من 900px) عبر `HeroPicture`: srcset/AVIF من `getImageProps` + `<link rel=preload media>` لكل مقاس؛ `<img>` واحد خام مستثنى في CH5 موثقاً. alt = alt الكتالوج (CH9)، والحاوية `aria-hidden` كالنموذج. | — | next/image لا يدعم `<picture>` |
| D118 | 2026-09-08 | **مصدر الصور = ملف مولَّد** `src/lib/imageCatalog.data.ts` من `catalog.json` عبر `scripts/import-catalog.mjs` (لا مسح لمجلد `public/images`، لا اشتقاق alt من اسم الملف). واجهة `imageCatalog.ts` العامة محفوظة فينتقل sitemap/ImageGallery تلقائياً. الصور في `public/images/catalog/` (مسطّح) والشعارات في `public/images/partners/`. | آلية `imageCatalog.ts` القديمة (مسح المجلد) | المرحلة 1 — PLAN.md |
| D119 | 2026-09-08 | صور الكتالوج التي تستهدف صفحات **لم تُبنَ بعدُ** (المرحلة 7: `/coffee-break-sharikat-jeddah` · `/diyafa-a3ras-jeddah` · `/qahwajiyat-sababat-jeddah`) تُؤوى مؤقتاً تحت `/portfolio` في image-sitemap كي تُفهرَس من اليوم؛ تنتقل تلقائياً عند بناء صفحاتها (القائمة في `EXISTING_PAGES`). | — | المرحلة 1 |
| D120 | 2026-09-08 | فاحص الأصول **CH9**: أي `<img>` من `/images/catalog/` في HTML المبني يجب أن يحمل alt الكتالوج **حرفياً** — تنفيذ آلي لـ D111. `ImageObject` يأخذ width/height الحقيقية من الكتالوج (كانت 1200×900 ثابتة كاذبة). | القيم الثابتة في `schema.ts` | المرحلة 1 |
| D127 | 2026-09-09 | **قالب الصفحة المحلية** `components/local/LocalPage.tsx` (خادم) + طبقة بيانات `lib/localPage.ts` (أنواع svc/city/intent) = `master_page` في النموذج v6.9 بـ 14 قسماً 1:1 (D115). المسارات الثلاثة `/[serviceCity]`، `/locations/[city]`، `/mubashirin-qahwa-jeddah` تستهلكه؛ حُذف `LocalServicePage/localContent/localImagery/RevealOnScroll`. og:image بأبعاد ونوع الصورة الحقيقيين (كان 1200×630 jpeg ثابتاً — خلل مُكتشف ذاتياً). | — | قالب واحد مصدر حقيقة |
| D128 | 2026-09-09 | **استثناء وحدات الهيكل من قياس التشابه** عبر `data-sim="chrome"` على الأقسام المتطابقة حرفياً في كل صفحة (شركاء، طاقم، تجهيزات، تواصل، تابعنا، بطاقات مكمّلة). `similarity-check` يزيلها بمطابقة متوازنة للوسوم ويطبع عددها (198 وحدة الآن). المبرر: نفس مبرر header/footer — ليست محتوى محلياً. | — | لا يُخفي المحتوى الأساسي |
| D129 | 2026-09-09 | **بوابة S12 بعد المرحلة 4 — مقاسة لا مُخمَّنة**: 185 زوجاً > 60% (خط الأساس 118). نفس الخدمة×مدن 84–94% (دَين قديم موروث)، مدينة↔مدينة 78–87%، **جديد**: خدمة↔خدمة أخرى 60–67% و مدينة↔خدمة 60–63% لأن القالب الموحّد يشارك نصوص «لماذا نحن/الأسئلة/الأحياء». الحل الجذري = المرحلة 5 (محتوى محلي عميق لكل صفحة). **لم يُحدَّث خط الأساس** بدون موافقة المالك؛ العمل مدفوع لفرع نسخ احتياطي `phase-4-wip` لا إلى main. | — | قاعدة «لا مخالفات جديدة» تُحترم |
| D130 | 2026-09-09 | **قرار المالك (رسالة 2026-09-09 «خليه يقبل الدمج»)**: الصفحات المحلية تبقى بمحتواها الحالي مؤقتاً، وخط أساس S12/التشابه يُحدَّث إلى 185 زوجاً (`UPDATE_BASELINE=1`) ليمرّ الدمج. يُلغي تعليق الدمج في D129. الدَّين يبقى موثّقاً: تنويع محتوى الـ 33 صفحة على مستوى الصفحة لا الخدمة (تحليل قسم-بقسم: roles/packages/why/faq/rel = 100% بين المدن). خط أساس «assets» **لم** يُحدَّث (بقي 27). | D129 | لا إخفاء دَين |
| D131 | 2026-09-09 | **خلل مُكتشف ذاتياً بالبوابة (CH9 ×99)**: شرائح الهيرو المحلية كانت بـ `alt=""` مخالفةً D111. الإصلاح: alt الكتالوج حرفياً + `aria-hidden` على حاوية الشرائح (زخرفية، كهيرو الرئيسية D126). axe = 0 بعده. | — | D111 ثابت |
| D132 | 2026-09-09 | **المرحلة 5 — الصفحات الرئيسية على النموذج v6.9 1:1** (D115): `components/pages/{shared,OfferingsPage,ServicesPage,PortfolioPage,AboutPage,ContactPage,LocationsPage,SocialPage}` + `/legal` مباشرة. طبقات بيانات `lib/{offeringsContent,servicesContent,portfolioContent}.ts` بصور الكتالوج فقط (D111) مع حُرّاس تحميل (لا تكرار · الصورة تستهدف الصفحة · لا زخرفي في معارض الخدمات). حُذفت الطبقة القديمة كاملة: `*Client.tsx` الخمسة، `ProtectedImage/ImageWithFallback/LocalReveal/LuxeIcons/Breadcrumbs/MotionProvider`، `lib/images.ts`، `luxe.css`، `local.css`، ومكتبة motion من المسار. | D120 | صفحة واحدة = قالب واحد + بيانات |
| D133 | 2026-09-09 | **حذف أرقام النموذج غير المؤكَّدة**: «15 خدمة» و«4.5★ من 49 تقييمًا» (من نحن/تواصل) غير موجودة بـ verified_by_owner في `data/proof.json` → لا تُعرض؛ بقي +500 · منذ 2016 · س.ت 7033069720 (رابط خرائط Google بلا رقم). | — | S10/E-E-A-T |
| D134 | 2026-09-09 | **التقديمات: فئات مُعاد تكوينها على الكتالوج** — النموذج استخدم 109 صورة منتجات غير موجودة في الكتالوج؛ الصفحة الآن 10 فئات + 24 قطعة معدات من 102 صورة حقيقية تستهدف /offerings، والزخرفي (ستوك، 32) يكمّل الفئات الفقيرة فقط وهو خارج الخريطة/الـ schema أصلاً. التوزيعات قسم نصي (لا صور توزيعات مستقلة في الكتالوج). التسميات = حقل service/title مشذّباً (إزالة بادئة/ترقيم) لا تسمية جديدة. خدمات بلا صور في الكتالوج (سوّاس · رسّام · مقدمات طعام · نظافة) تُعرض بلا معرض. | — | لا ستوك ولا استعارة |
| D135 | 2026-09-09 | **إصلاحات مُكتشفة ذاتياً**: (أ) الواتساب الطافي كان يُربط مرة واحدة ولا يعرف `.phero` → يُعاد ربطه مع المسار ويراقب `.hero, .phero`. (ب) نقل CSS بمطابقة سطر-بسطر أسقط `}` منفردة → بناء فاشل؛ أُصلح وأُضيف عدّ الأقواس للتحقق. | — | — |
| D136 | 2026-09-09 | **خلل مُكتشف بالفحص البصري (phero)**: طبقة التعتيم `::before` كانت تحت حاوية الصورة `.bg` في التراص → عنوان الصفحة على صورة فاتحة بلا قراءة. الإصلاح: `::before{z-index:1}` و`.wrap{z-index:2}`. فحوص المرحلة 5: Playwright 32/32 (8 صفحات × 4 مقاسات) · axe 0 · Lighthouse mobile تقديمات 54→67 دافئ / خدمات 50→69 دافئ (نفس نمط الرئيسية: محسّن الصور بارد + gtag TBT ≈1.1s) — المرحلة 6. | — | — |
| D137 | 2026-09-09 | **إصلاحات من رسالة المالك الصوتية (لقطات جوال الخدمات/تواصل)**: (أ) السبب الجذري لبطاقة الخدمة المضغوطة: نقل CSS بمطابقة سطر-بسطر أسقط سطر `@media (min-width:900px){` (كان موجوداً سابقاً) فسرت قواعد الحاسوب (`.card{display:grid;grid-template-columns:1fr 1.05fr}`) على الجوال → نص بعرض 131px وصور 30px. أُعيد بناء الكتلة بحفظ الأسطر الهيكلية (`@media`/`}`) دائماً. (ب) أيقونات قنوات التواصل: `sizes="56px"` مع next/image أنتج srcset لا يختاره المتصفح داخل الحاوية → فارغة؛ الأيقونات (≤15KB) تُخدم `unoptimized`. (ج) زرا البطاقة/الهيرو في صف واحد على الجوال (`flex:1 1 0` · `nowrap`) بطلب المالك. | D135 | لا تعديل على النموذج في الحاسوب |
| D138 | 2026-09-09 | **فحص بيكسل-بيكسل آلي** `scripts/pixel-audit.mjs` (Playwright، 390px، 11 صفحة): نص < 11px · فقرة ضيقة · صورة محتوى < 60px · أزرار متراصة · تمدد أفقي. يُضاف إلى بوابة ما قبل الدمج بجانب axe. النتيجة الآن: 0 نص ضيق · 0 صور صغيرة · 0 تمدد؛ المتبقي المعلوم: وسوم `.tag` 10.9px و`.l2` 9.9px (تصميم النموذج — قرار مالك إن أُريد تكبيرها). | — | — |
| D139 | 2026-09-09 | **ملاحظة المالك (صوتي) — دَين مراجعة محتوى/صور، لا تُنفَّذ الآن بأمره «أكمل البناء أول شيء»**: (أ) بطاقة «كركديه مثلج» في التقديمات تستعمل ستوك زخرفي #307 (المالك: هذا شاي أحمر) — الستوك الزخرفي لا يُعرض كتقديم محدد الاسم. (ب) بطاقة «تقديم قهوة تركية — بطاقة QR» (#108) — وصف الكتالوج للفهرسة، أما العرض في الموقع فبما يناسب القسم: لا QR ولا شعارات مكررة في التقديمات. (ج) قاعدة مستقبلية: اسم البطاقة/الصورة في الموقع = ما يناسب القسم، وalt/title الكتالوج يبقى حرفياً (D111) للفهرسة. التنفيذ بعد اكتمال المرحلة 6 وبمراجعة المالك بطاقة-بطاقة. | — | لا تعديل الآن |
| D140 | 2026-09-09 | **حزمة العميل بلا كتالوج**: `ContactForm`/`PortfolioFilters` (client) كانا يستوردان `servicesContent`/`portfolioContent` فيُسحب `imageCatalog.data.ts` كاملاً (chunk 172KB) إلى المتصفح في `/contact` و`/portfolio`. القاعدة: مكوّنات العميل تأخذ بياناتها props من مكوّن خادم، ولا تستورد وحدات المحتوى مباشرة. | — | يُفحص بعد كل بناء: `ls .next/static/chunks` بلا chunk > 100KB غير framework/main |
| D141 | 2026-09-09 | **تأجيل محمِّل gtag.js (GA4+Ads)**: مقتطف `dataLayer` + `gtag('config', G-…/AW-…)` يبقى حرفياً في `<head>` (شرط اكتشاف Google Ads الآلي — سبب العطب القديم af96b9a)؛ المحمِّل الخارجي (~350KB) يُحقَن بعد `load`+2.5s ثم `requestIdleCallback(≤3s)`، أو عند أول تفاعل (pointerdown/keydown/touchstart/scroll) — أسبق الاثنين، لكل زائر. الأحداث قبل التحميل تُصفّ في dataLayer فلا تُفقد تحويلات. النتيجة: TBT 1,370→380ms · BP 77→100 على `/services`. | — | إن ظهر تحذير «لا تتوفر علامة» في Google Ads خلال أسبوع: يُعاد `<script async>` فوراً ويُوثَّق |
| D142 | 2026-09-09 | **شبكة `.eq` (مقصوصات العدّة) في الصفحات المحلية** — النموذج v6.9 §7 (`local.py CUTOUT_ITEMS`): 6 قصاصات 480×480 (`dallah-gold · cup-stripes · cup-emblem · cup-faceted · cup-porcelain · dallah-silver`) بأسمائها حرفياً، من `img/cutouts/` المُحسَّنة (10–25KB بدل 17–66KB). ليست صور كتالوج (عناصر تصميم) — alt = الاسم المعروض. يُسدّ دَين المرحلة 4. | — | — |
| D143 | 2026-09-09 | **فيديو الهيرو بعد `load`**: كان `requestIdleCallback` يُطلق التحميل قبل اكتمال الصورة/الخطوط فينافس 240KB الموارد الحرجة على الجوال. الآن `load` → خمول → تشغيل. الرئيسية (بلا gtag) 80→87. | — | — |
| D144 | 2026-09-09 | **حذف الصور القديمة** (ينفّذ D120): 18 مجلداً · 333 ملفاً · ~31MB في `public/images/*` لا يشير إليها أي كود (الفحص: grep على src/scripts/public بلا imageCatalog.data). public 61→29MB. خط أساس assets 27→11 (`UPDATE_BASELINE=1` — تخفيض لا زيادة). بقي: brand/badges/catalog/cutouts/links/partners. | D120 | — |
| D145 | 2026-09-09 | **`/links` صفحة الروابط المستقلة** (كانت في خطة المرحلة 5 ولم تُبنَ — اكتُشفت في تدقيق «هل خلص البناء»): النموذج v6.9 `build_links` 1:1 — الشعار + 9 بطاقات + `.lfoot`، **بلا** هيدر/فوتر/واتساب طافٍ/معرض/نموذج. التنفيذ: مجموعتا مسارات `src/app/(site)/` (كل الصفحات + `template.tsx` الشِل) و`src/app/(bare)/` (layout بلا ClientLayout)؛ `not-found` يلفّ ClientLayout صراحةً. **الفهرسة: `index, follow`** (النموذج يضع noindex؛ PLAN §5 قرر «تُفهرس — وجهة الباركود المطبوع» ونتبع PLAN). CSS `LINKS_CSS` بـ`main.links-page` بدل `body.links-page`. في sitemap بأولوية 0.5. | — | مسار جديد — لا يخالف «لا تغيير slug» |
| D146 | 2026-09-09 | **إغلاق دَين D101 + فحص متصفح شامل مُلتزَم**: (أ) تأثير الأومنتريكس صار مكوّناً مستقلاً `ChannelFx` يُستخدم في `/contact` و`/links` (كان داخل ContactForm). (ب) `scripts/browser-sweep.mjs` = كل مسارات sitemap × 4 مقاسات + axe @390 (D117)، و`scripts/html-audit.mjs` = canonical/robots/JSON-LD/h1/og لكل مسار. النتيجة على 42 مساراً: **168/168** بعد إعادة فحص 23 علامة `brokenImgs` (كلها كاذبة: صور غير كسولة لم تكتمل ضمن 800ms على محسّن صور بلا CDN — إعادة الفحص بانتظار `load` = 0 مكسورة)، axe **0** على 42 صفحة، HTML **43/43**. | — | `brokenImgs` في السويب يُقرأ مع recheck لا وحده |
| D147 | 2026-09-09 | **تسليم لوكيل جديد — نقطة دخول واحدة `AGENTS.md`** (بطلب المالك: «يقرأ كل شيء كأنه أنت وذاكرتك»): ترتيب قراءة إلزامي (AGENTS → AGENT-STATE → PLAN → DECISIONS → owner-messages كلها → آخر تقرير → BOOTSTRAP) + ذاكرة الوكيل غير المكتوبة في الكود (سؤال D138 المعلّق، مبدأ المالك في D139، أسلوب الخطاب، سقوط التوكن، بيئة 3111). الملفات التاريخية (CLAUDE.md · AI_INSTRUCTIONS · ROADMAP · تقارير الجذر ≤2026-09-08 · audit/ · reports/) عُلِّمت «لا تُعتمد» بشارة في رأسها ولم تُحذف (أرشيف). سكربتات الفحص المؤقتة من `/tmp/pw` نُقلت إلى `scripts/browser/` مع README. | — | يُحدَّث AGENTS.md في نهاية كل جلسة |
| D148 | 2026-09-11 | **كل تقرير بحث/تحليل يحمل سطر مؤلف** في رأسه: اسم الوكيل (Claude — Anthropic) + الجلسة + «بأمر المالك MoTechSys» + النوع (تحليل فقط / تنفيذ) + المنهج. أُضيف رجعياً للتقارير الثلاثة ليوم 2026-09-11. المسؤولية عن الأرقام على الوكيل، والقرارات المُعلَّمة «قرار المالك» لا تُتَّخذ بدونه. | — | بأمر المالك: «وثق بحوثاتك باسمك في التوثيق» |
| D149 | 2026-09-11 | **خصوصية + موافقة كوكيز (PDPL) بـ Consent Mode v2**: `gtag('consent','default', denied…)` قبل `config` في `layout.tsx` (+`url_passthrough`، `ads_data_redaction`) → gtag يعمل بلا كوكيز حتى الموافقة؛ شريط `CookieConsent.tsx` (موافق / الضروري فقط) يحفظ الاختيار في `localStorage.kd_consent` ويرفع الإشارات إلى granted؛ صفحة `/privacy` (قالب /legal) تصف ما يُجمع فعلاً (GA4 + Ads فقط، واتساب بلا تخزين، حقوق PDPL)؛ رابطها في الفوتر. CSP: أُضيف `pagead2.googlesyndication.com` (وجهة ccm/collect في وضع الموافقة — كان يُحجب، قِيس بالمتصفح). **قاعدة:** أي أداة تتبّع جديدة تُذكر في `/privacy` قبل تفعيلها. | — | أمر المالك «كوكيز ليش مافيش معانا… اصلح» · G3 |
| D150 | 2026-09-11 | **sitemap lastmod حقيقي**: كل التواريخ من `git log -1 --format=%cs` لملفات القسم (42 صفحة = 2026-09-09، /privacy = 2026-09-11) بدل يوليو الكاذب؛ `sitemap_index` lastmod ثابت لا `new Date()`؛ **`/links` خارج sitemap** (يتيمة 0 روابط واردة + نصّ مكرر 100% مع /contact) لكن تبقى index,follow (وجهة الباركود المطبوع — D145). | يعدّل D145 جزئياً (الفهرسة تبقى، الخريطة لا) | G4 · تحليل الهيكلية §1.2 S-1 |
| D151 | 2026-09-11 | **نظافة التبعيات**: حذف `motion` و`embla-carousel-react` (0 استخدام) من package.json و`optimizePackageImports` · حذف `DallahLogo.tsx` (0 استيراد) · `tsconfig.tsbuildinfo` أُخرج من التتبّع (كان في .gitignore ومتتبَّعاً معاً). | — | G13 |
| D152 | 2026-09-11 | **مجمّع صور القالب المحلي**: (1) صور الصفحة المستهدفة في الكتالوج **أولاً بلا تدوير** (القديم كان يُزيحها لصالح المستعارة: مباشرين جدة عرضت 2 من 16 → الآن 16/16، قهوجيين مكة 4/15 → 15/15)؛ (2) 16 صورة **مختلفة** (كان 13 مع تقاطع roleImgs/shots → 7 ملفات مكررة في الصفحة)؛ (3) إزاحة المستعار موزَّعة بالتساوي على المدن (القديم `seed*5 % len` أعطى مكة = الطائف 100% تطابقاً). النتيجة المُقاسة: تطابق الصور المتغيّرة بين الشقيقات 14–32% (كان الكل 60%؛ الثابت المشترك 21–22 صورة من أقسام staff/offerings/services بحكم القالب). | — | تحليل الصور §2.2–2.4 |
| D153 | 2026-09-11 | **ترتيب الجذر**: 12 تقرير قديم (≤2026-09-08) + `manus-updates/` + `agent-skills/` → `docs/archive/` مع README؛ `docs/DEPLOYMENT.md` أُعيد كتابته بالكامل (كان يصف `SocialClient.tsx` المحذوف ومعرّفات «بالتخمين» صارت متحقَّقة). `audit/` `reports/` `research/` بقيت (مُشار إليها من تقارير سابقة). | — | G12 · أمر المالك «اصلح ايش خراب وغير مرتب» |
| D154 | 2026-09-11 | **ميتا الجذر الافتراضية**: حذف «أفضل صبابين قهوة» من description الافتراضي في layout.tsx (ادّعاء تفوّق — يخالف سياسة Google Ads وروح S10؛ كان يظهر في 404 فقط) · تصحيح `twitter.creator/site` من `@keifdiafa` (حرف ناقص) إلى `@keifaldiafa` المتحقَّق في `site.ts` (layout + SEO.tsx). | — | تحليل المحتوى §3.4 |
| D155 | 2026-09-11 | **تحديث خطّي الأساس seo/similarity 185→178** (UPDATE_BASELINE=1): D152 غيّر تركيبة أزواج التشابه (40 زوجاً خرجت، 33 دخلت — كلها في نطاق 60–61% حول العتبة، والمتوسط 77.0→77.4% أي **بلا تغيير جوهري**، والأقصى 94.5→95.1%). التكرار الحقيقي نصّي (7–8% كلمات فريدة) وحلّه المرحلة 7 لا الصور. | — | قاعدة baseline: لا مخالفات جديدة إلا بتحديث موثَّق |
| D156 | 2026-09-18 | **منهجية المرحلة 7 (بأمر المالك الصوتي/النصي 2026-09-18):** (1) **5 صفحات نيّة** تُبنى كلها ثم تُنشر **دفعة واحدة** (لا نشر مرحلي): `/qahwajiyat-sababat-jeddah` · `/diyafa-alyawm-alwatani` · `/qahwajiin` · `/coffee-break-sharikat-jeddah` · `/diyafa-a3ras-jeddah`. (2) **اسم الصفحة/H1/العنوان = ما يكتبه الباحث حرفياً** («ضيافة اليوم الوطني» لا «اليوم الوطني كيف الضيافة»). (3) **قاعدة الإملاء المقاسة**: قهوجيين/صبابين/مباشرين/قهوجيات/صبابات/مباشرات فقط في النص المرئي؛ **قهوجيون/صبابون/مباشرون ممنوعة** (0 بحث مُقاس في GSC+Ads) — فحص آلي S15 في seo-guard. (4) كل صفحة فرعية = **صفحة رئيسية كاملة** (14 قسماً + معرض صور إضافي ≥16 صورة كتالوج فريدة، محتوى يدوي ≥1,500 كلمة، Service+WebPage+Breadcrumb+ImageGallery). (5) هدف التشابه لكل صفحة جديدة **≤45%** (PLAN §7). (6) Vercel مربوط بـ `keif-v2` (تأكيد المالك). (7) الصور من الكتالوج فقط بترتيب: المستهدِفة للصفحة → المستعارة من الأخوات → احتياط tier≤3؛ صفحة اليوم الوطني تختار صورها بمعرّفاتها (13 صورة موثّقة §6 تقرير 2026-09-18ب). | يكمّل D116 · D127 · D152 | صوت المالك ×5 + أمر البدء 2026-09-18 · `docs/owner-messages/2026-09-18-*` |
| D157 | 2026-09-18 | **تحديث خطّي الأساس seo/similarity 178→192** (UPDATE_BASELINE=1) بعد D156: تصحيح الإملاء في النصوص المشتركة (قهوجيون→قهوجيين ×13) أزاح 16 زوجاً قديماً من صفحات (خدمة×مدينة) إلى 60.0–61.2% حول العتبة — **لا زوج منها يشمل الصفحات الخمس الجديدة**؛ المتوسط 77.4→76.0% (تحسّن) والأقصى 95.1% بلا تغيير. الصفحات الجديدة: أقصى تشابه 57.0% (قهوجيات↔زواجات — تشتركان في القسم النسائي) و≤50.3% للباقي. التكرار الحقيقي يبقى في الـ24 صفحة المدينية = المهمة التالية. | يكمّل D155 | قياس مباشر computePairs قبل/بعد على d409d07 |
| D158 | 2026-09-18 | **أحياء جدة بترتيب الطلب المُقاس لا الانطباع:** قائمة `CITIES.jeddah.districts` أُعيد ترتيبها وتوسيعها (8→18) بحسب ما يكتبه الباحث فعلاً مع «قهوجي» في GSC 16 شهراً + Ads منذ 2023: الفيحاء 207 ظهوراً · المحمدية 164 · الزهراء 116 · المرجان 84 · البلد 48 — **لم يكن أيٌّ منها في القائمة القديمة**. القاعدة: لا صفحات أحياء (الطلب الإجمالي 822 ظهوراً لكل الأحياء معاً = دون عتبة صفحة مستقلة، والكتالوج لا يملك ≥6 صور لأي حي)؛ الأحياء تُذكر في قسم التغطية وفي الإعلانات فقط. | يكمّل D156 | `data/search-intel/2026-09-18/` قياس 59 مصطلحاً حياً |
| D159 | 2026-09-18 | **معجم القهوة: «القهوة السعودية» (رسمي منذ 2022 — وزارة الثقافة/التجارة، مصدر: saudipedia) تُستخدم في السياقات الرسمية والتراثية (اليوم الوطني · الدليل · من نحن) إلى جانب «القهوة العربية» التي يكتبها الباحث 3:1 (129 مقابل 39 ظهوراً مُقاساً)** — لا استبدال شامل. أُضيفت معلومات موثّقة: تسجيل اليونسكو 2015 · مصطلح «الحمصة» واختلاف لونها بالمناطق (ذهبية وسط · داكنة شمال · فاتحة جنوب · معتدلة غربية بمستكة) · أصول الصبّ (الدلة باليسرى والفناجين باليمنى، ثلث الفنجان، الصدارة ثم اليمين، الوقوف حتى انتهاء الضيوف). المصدر يُذكر في التقرير لا في الصفحة. | يكمّل D156 | saudipedia.com/القهوة-السعودية · kitchen.sayidaty.net · jordanheritage.jo (أدب القهوة) |
| D161 | 2026-09-22 | **قواعد كتابة المرحلة 7 المُقاسة (W1–W9)** من تقرير `docs/reports/2026-09-22-competitors-google-standards-academic-geo-research.md`: رقم موثّق واحد/قسم (proof.json فقط) · اقتباس بمصدر رسمي · لا تكرار كلمة مفتاحية >1/فقرة · جدول/قائمة قابلة للمسح · جملة تبرير «لأن…» قابلة للاقتباس لكل خدمة · **تطابق النص مع الصورة** (قسم محلي لا يعرض صور مدينة أخرى بادّعاء محلي) · H2 وصفية لا سؤالية (الأسئلة في FAQ فقط) · الفريد لكل مدينة = زاوية مُقاسة من GSC · **لا صفحة لتنويعة بحث** (قهوجيين جده/قهوجي في جدة تُخدم داخل الصفحة). المصادر: Google AI guide 2026-07-10 · Aggarwal KDD 2024 (+32–41% إحصاءات/اقتباس، −10% حشو) · Chen 2025 (Earned 69–94%) · مراجعة 45 دراسة 2026-07 (الصلة والموضع فقط قابلان للتكرار) · JHTT 2025 (تطابق نص↔صورة→ثقة). تُطبَّق على جدة/ينبع/المدن و/locations قبل خط الأساس الجديد. | يكمّل D156 · D160 | أمر المالك 2026-09-22 «قبل أن تبدأ حلّل المنافسين والمعايير والبحوث» — كل قاعدة بدليل مُقاس في التقرير §4/§7 |
| D162 | 2026-09-22 | **تغطية المدن الثماني بنصوص يدوية + خط أساس 28** (تنفيذ أمر المالك «انطلق بدقة وعمق»): (1) `src/lib/local/jeddah.ts` و`yanbu.ts` مكتوبان يدوياً بقواعد D161 وبلهجة الباحث المُقاسة من GSC/Ads (بجدة · قهوه · جده · زواجات · قهوجية · مباشرين · «بكم» في FAQ) — جدة من أرشيف صورها المُقاس (أمانة جدة/معرض الحج/مافيرا/قصر التنين)، وينبع صريحة «نصل من جدة بحجز مسبق، لا فرع» لأن طلبها المُقاس ضعيف (قهوة ينبع 23 ظهوراً). (2) `getCityPage` يقرأ الأدوار من النص اليدوي لا من القالب، و`cities.ts` (intro/body/highlights/faqs) أُعيدت كتابته للمدن الثماني بزاوية فريدة لكل مدينة (intro = meta description 152–159). (3) أبها: 3 عناوين ≤46 ووصفان ≤158؛ وصف ضيافة المدينة/الرياض/الطائف ≥120. (4) T2: `hasMap` + `hasCredential`×3 (الشهادات بأسمائها الحرفية) + `knowsAbout`؛ `founder` بلا تغيير (A3 للمالك). T3: `SOCIAL.googleReview` (writereview?cid) كدعوة نصّية في /about و/contact بلا AggregateRating. T4: H2 السؤالية في القالب (3) وصفحات النيّة (7) → وصفية؛ الأسئلة تبقى في FAQ. T1 مؤجَّل: صفحة preferences/source تُرجع 200 بلا محتوى للزاحف — يُفحص من متصفح المالك. (5) خطّا الأساس seo وsimilarity **192 → 28** (كلها S12 في 60–71%؛ الأعلى ضيافة المدينة↔مكة 71.36%؛ جدة↔ينبع من 90.9% إلى <70%)؛ assets 11 بلا تغيير. البنية v6.9 1:1 (D115) والـslugs بلا تغيير؛ الرئيسية («من كل شي شوية»): أُضيف صف «الأكثر طلباً في جدة» في شريط المدن (خدمات جدة الثلاث + مباشرين قهوة) لأنها لم تكن تربط بأي صفحة خدمة×مدينة؛ بقية الأقسام بلا تغيير. | تُغلق المرحلة 7 نصّياً؛ لا نشر إلا بأمر المالك. |
| D163 | 2026-09-22 | **إعادة بناء صور الكتالوج الـ314 من أصول keif-v2 النظيفة بختم واحد** (تنفيذ أمر المالك «الشعار على كل صورة بحجمها… قصّ الطوال والعراض… ضغط ذكي… ويب… جهّزهن للفهرسة»): (1) المصدر أصل keif-v2 لكل سجل وفق `2026-09-22-image-sources-mapping.csv` (314/314). (2) **ختم واحد**: الشعار الرسمي (`scripts/images/assets/wm-logo.png` من مستودع images-watermarked) في أهدأ ركن مقاس لكل صورة، حجم تكيّفي 6.1–12.5% من العرض، تفتيح على الداكن؛ **بلا ختم** 36 صورة تحمل الهوية أصلاً (قالب المنتجات · شارة K · هيرو · بانرات مصمّمة). لا طبقة مركزية. (3) قصّ ذكي (حواف+وجوه) لـ10 صور خارج 9:16–16:9؛ #99 يبقى بانورامياً للهيرو؛ البانرات لا تُقصّ. (4) أطول ضلع ≤1600 (هيرو 1920)؛ 17 لقطة مشوّشة → 1200 بدل ضغط <40dB. (5) WebP بجودة مختارة لكل صورة (PSNR ≥42dB · سقف 240KB): 0 فوق CH1. (6) **XMP** في كل ملف من الكتالوج حرفياً (title/alt/creator/rights/subject/WebStatement/Licensor). (7) width/height/kb مُحدَّثة في `imageCatalog.data.ts` (v5)؛ **file/alt/title/pages بلا تغيير** (D111) ولا slug. الأسماء لم تُغيَّر رغم ذكر المالك «إعادة التسمية» لأنها وصفية ومربوطة بالفهرسة القائمة. السكربت `scripts/images/rebuild-catalog.py` قابل لإعادة التشغيل؛ لاستبدال الرسمة بالبيضاوية يكفي تبديل ملف الشعار. **مكمّل (نفس اليوم):** `CATALOG_ASSET_VERSION="5"` تُلحق `?v=5` بـ`src` صور الكتالوج فقط (لا بـurl الخريطة/schema/og) لأن الاستبدال بنفس الأسماء مع `Cache-Control: immutable` أبقى المتصفح على النسخ القديمة — المالك رأى الشعار المكرّر في المعاينة بعد التنفيذ؛ تُرفع النسخة عند كل إعادة بناء للصور. البوابة: tsc/lint/build ✓ · guard 28/28 · assets 11/11 · html 48/0. التقرير: `docs/reports/2026-09-22-catalog-rebuild-single-watermark.md` + `…-results.csv`. | غير منشور — النشر بأمر المالك. |
| D164 | 2026-09-22 | **إلغاء D163 وإعادة نمط الشعار السابق**: بأمر المالك («ما أشتي الشعار هذا… السابقات اللي فيها شعار واحد أستخدمها تماماً… المكرّرة أعدها من الأصل بواحد شعار في الوسط»). (1) الـ139 صورة السليمة **مستعادة بالبايت** من `5bdef6d`. (2) الـ175 المكرّرة من أصل keif-v2 بنفس أبعادها السابقة + **الشعار البيضاوي** (`keif-v2/public/images/watermarks/svg/logo-1.svg` → `scripts/images/assets/wm-oval.svg`) مرّة واحدة في الوسط بمواصفة **مقاسة من الـ139**: عرض 34% (28% للعريضة) · مركز y 61% (58%) · عتامة 0.65؛ 6 بلا ختم إضافي (هيرو 2 · بانرات 4). (3) الشعار الرسمي في الزاوية أُزيل من كل الصور ومن الأصول. (4) `CATALOG_ASSET_VERSION=6`. (5) خط أساس assets 11→13 (CH3: #133/#134 نفس الأصل → ملف واحد؛ #112/#131 نفس الأصل) — حذف السجل المكرّر قرار مالك (D139). السكربت الساري `scripts/images/restamp-double.py`. **قاعدة مستخلصة:** الشعار وموضعه قرار هوية للمالك — عيّنة قبل أي تنفيذ جماعي. التقرير `docs/reports/2026-09-22-restamp-double-to-single-center.md`. | غير منشور — النشر بأمر المالك. |
| D165 | 2026-09-22 | **فحص آلي للصور ضد أصولها + قاعدة «لا شعار فوق وجه»** (بأمر المالك بعد أن أرسل #95 شعار فوق وجه و#5 شعارين): (1) `scripts/images/audit-stamps.py` يعدّ نسخ الشعار البيضاوي من فرق البكسل عن أصل keif-v2 (مطابقة قالب متعدّدة المقاسات + NMS) ويكشف الوجوه (YuNet) ويحسب التغطية — فكشف 10 صور بشعارَين فاتت الفحص بالعين، 45 شعارها على وجه، 16 بلا شعار. (2) `restamp-double.py`: موضع الشعار يبدأ من الوسط ويتحرّك (نزولاً ثم جانبياً ثم تصغير خطوة) إلى أول موضع لا يغطي >5% من أي وجه. (3) النتيجة المقاسة 314/314: **0 شعارين · 0 فوق وجه · 305 شعار واحد · 9 بلا ختم عن قصد**؛ 101 مستعادة بالبايت، 213 من الأصل. (4) `?v=7` · CATALOG_VERSION v7 · خط أساس assets 13→21 (10 أزواج سجلات بنفس الأصل → دمجها قرار مالك D139). (5) تحليل الموقع على الهاتف بالمعايير (WCAG 2.2 · Apple HIG · CWV) في التقرير §6–8 — 7 توصيات **بانتظار قرار المالك**. **قاعدة ملزمة:** أي تغيير على الصور يُتبَع بـ`audit-stamps.py` ولا يُسلَّم إلا بـ0/0. التقرير `docs/reports/2026-09-22-image-audit-and-mobile-ux.md` + `…-image-audit-results.csv`. | غير منشور — النشر بأمر المالك. |
| D166 | 2026-09-22 | **بطاقة الصورة = سطر واحد** (بأمر المالك: «2 نصوص… هل يصح أو تشوّه» → «حسّن كل شي بأعلى المعايير»). حقل مشتق `caption` في `CatalogImage` من `title` بقواعد **حذف فقط** («ضيافة كيف الضيافة في»، «— كيف الضيافة»، «— N»، «— صورة زخرفية») — لا كلمة مُخترَعة؛ `title`/`alt` كما هما (D111/CH9)؛ الوصف الكامل يبقى في المعرض المكبَّر. 5 مواضع `figcaption`. تغطية النص للصورة 0.40–0.79 → 0.24–0.36. أثر جانبي موثّق: زوجان محليان تجاوزا 60% تشابهاً بفارق <1% (مكة↔ينبع · أبها↔الرياض) → خطّا الأساس seo/similarity 28→22. | `<b>{title}</b><span>{service}</span>` فوق الصورة | `docs/reports/2026-09-22-mobile-standards-D166-D170.md` §2 |
| D167 | 2026-09-22 | **شريط الكوكيز لا يغطي CTA الهيرو**: كان top 602/h 154 فوق الأزرار 629–683 (390×844)؛ الآن top 697/h 135 تحتها، عرضه يترك 86px لزر واتساب، أزراره 44px. سطح المكتب كما كان. | تغطية أزرار الهيرو في أول زيارة | التقرير §3 |
| D168 | 2026-09-22 | **أهداف اللمس ≥44px على الجوال** (WCAG 2.5.5 / Apple HIG) **وخطوط المعلومات ≥13px**: breadcrumb 23→44 · روابط الرأس 39→44 · فلاتر/وسوم 37→44 · btn-sm 40→44 · روابط الفوتر 25→44 · الهاتف 18→44 · أيقونات التواصل 38→44. أهداف 24–43px: 53–71 لكل صفحة → **0** في 7 صفحات مقاسة. الزخرفية اللاتينية (8.6/9.9px) مستثناة عمداً. كتلة CSS واحدة موثّقة في نهاية `v7.css` — أسطر النموذج الأصلية لم تُعدَّل (D115/D121). | القيم الأصلية للنموذج على الجوال | التقرير §4 |
| D169 | 2026-09-22 | **`btn-glass` بخلفية داكنة ثابتة** `rgba(13,13,13,.55)` (0.62 في الهيرو) + حدّ 0.6 → التباين لا يعتمد على صورة الخلفية (≥ ~9:1). تصحيح D165: `btn-gold` 10.5:1 ✅ (قياس D165 كان خاطئاً). | `rgba(255,255,255,.04)` | التقرير §5 |
| D170 | 2026-09-22 | **بلاطات الزيّ/التقديمات أكبر على الجوال** (46→58% · 42→52% من العرض) — القياس الدقيق 8–9 صور/شاشة لا 51 (تقدير D165 عدّ النسخ المستنسخة). البنية والعدد وشريط v6.9 كما هي. | البلاطات الصغيرة | التقرير §6 |
| D171 | 2026-09-23 | **بطاقة معاينة (og:image) مصمَّمة لكل صفحة — 49/49** (بأمر المالك: «لكل صفحة صورة معاينة مختلفة… بالذكاء الاصطناعي… ويب خفيفة… حسب المعايير»). 1200×630 WebP 60–118KB (WhatsApp يتجاهل >300KB)؛ خامة ذهبية داكنة مولَّدة بالذكاء الاصطناعي + صورة حقيقية **مختلفة لكل صفحة** من الأصل النظيف (بلا ختم) بقصّ يتجنّب الوجوه + عنوان الصفحة Amiri + الشعار + «+500 · منذ 2016». `ogImageFor(path)` في `SEO.tsx` تُعطي البطاقة الأولوية تلقائياً؛ `OG_VERSION` يكسر كاش المكشّطات. **حارس CH10** في `check-assets.mjs` يفرضها لكل صفحة مبنية. بعد النشر: Scrape Again في Facebook Debugger + `@WebpageBot` لتيليجرام. | `og-cover-v2.jpg` واحدة لـ7 صفحات + صور هيرو مكرّرة (5 صفحات تتشارك صورة واحدة) | `docs/reports/2026-09-23-og-images-D171.md` |

## ملاحظات تنفيذية مرتبطة
- المرحلة 1: الصور القديمة في `public/images/**` (349 ملفاً) **باقية** لأن الصفحات البصرية القديمة (HomePageClient/Offerings/Services/Portfolio/localImagery) ما زالت تشير إليها؛ تُحذف مع إعادة بناء كل صفحة في المراحل 3–5. حُذف 10 ملفات فقط لا يشير إليها كود.
- المرحلة 1: `ProtectedImage showWatermark` (تركيب شعار برمجي — يخالف D113) أُزيل من صور الكتالوج في `locations/[city]`؛ ما بقي منه على الصور القديمة (Portfolio/Offerings) يزول مع المرحلة 2.
- المرحلة 3 ✅: `PartnersMarquee` حُذف — شبكة الشعارات الكريمية (46 من `partners.ts` بالأولوية + خانة «+500») في `HomePage`. `ProtectedImage` (بقايا العلامة البرمجية على الصور القديمة) يزول مع المرحلتين 3 و5.
- المرحلة 2: Lighthouse جوال على الرئيسية (خط أساس قبل المرحلة 3): Performance **40** (LCP 8.7s · TBT 1.9s — جسم الصفحة القديم بأشرطة motion) · A11y **100** · BP 77 (third-party-cookies من gtag) · SEO **100**. الهدف ≥90 يُقاس بعد المرحلة 3.
- ملاحظة لوكيل التحليل (خارج صلاحية وكيل البرمجة — D111): 4 alt في الكتالوج تحوي أقواساً (#53 #202 #207 #208) وتخالف قاعدة «·» بدل الأقواس.
- CSS النموذج يُنقل 1:1 إلى `src/styles/v7.css` (لا Tailwind لهذا الجزء) — الدقة البصرية أهم من إعادة الكتابة. (سيُرقَّم عند التنفيذ في المرحلة 2)
- الخطوط: `next/font/local` بملفات woff2 المُقلَّصة من النموذج. (المرحلة 2)
- `/links` مسار **جديد** يُضاف — لا يخالف قاعدة «لا تغيير slug». (المرحلة 5)
- فاحص الأصول حصل على **CH9**: أي `<img>` لصورة كتالوج في HTML المبني يجب أن يحمل alt الكتالوج حرفياً (D111) — يفشل البناء عند أي تركيب نصّي. (المرحلة 1)
- ملاحظة لوكيل التحليل (لا يغيّرها المبرمج): 4 alt منشورة تحوي أقواساً «( )» — الأرقام 53 · 202 · 207 · 208 — تخالف قاعدة «·» بدل الأقواس في README الكتالوج.
- المرحلة 3: Lighthouse جوال الرئيسية (محاكاة Lantern، chromium 1134): Perf **72** (من 40) · A11y **100** · BP 77 (third-party-cookies من gtag — خارج الكود) · SEO **100**. LCP 3.2s محاكى (المرصود 0.53s؛ Lantern يضيف ~2.7s لسلسلة الخطوط/CSS على 4G بطيء). التفصيل: بلا gtag 78 · بلا سلاسل JS 75 → التحسين المتبقي في JS المشترك (react-dom 55KB + 117 33KB) وgtag، وهما خارج نطاق الرئيسية — يُعالَجان في المرحلة 6 (الفحص الشامل). هدف ≥90 **لم يتحقق بعد** ومسجَّل كدَين.
- المرحلة 3: `imageCatalog.ts` يكشف `entity/service/place` من الكتالوج (حرفياً) لاستعمالها في تعليقات المعرض (data-sub) — لا نص مؤلَّف عن الصور (D111).
- المرحلة 3: فيديو الهيرو `public/video/hero-bg.mp4` (240KB · H.264 720×1280) من النموذج؛ preload=none ويُحمَّل بعد الخمول. Chromium مفتوح المصدر في Playwright بلا H.264 فلا يُختبر التشغيل آلياً (يعمل في Chrome/Safari).
- المرحلة 3: صور النموذج القديمة (`img/photos/*`) طُوبقت بالبصمة الإدراكية dHash لاختيار مقابلها من الكتالوج؛ الستوك الزخرفي في «التقديمات» استُبدل بصور أعمال حقيقية `publish=yes` من نفس الفئة (#110 #239 #245 #231 #227).

## D160 — 2026-09-18 — المرحلة 7 (ج): روابط سياقية · /about جديد · تشابه قهوجيات↔زواجات ≤45% · نصوص يدوية لـ 6 مدن × 3 خدمات
- **روابط سياقية:** مكوّن `IntentLinks` في `src/components/pages/shared.tsx` يُضاف في /services /offerings /portfolio /about، وشريط «وبحسب المناسبة» في الرئيسية (`HOME_INTENTS`). النص = `INTENT_PAGES.ar` (كلمات الباحث).
- **/about أُعيدت كتابته** (`AboutPage.tsx`): قصتنا → كيف نعمل (4 خطوات) → من نخدم (3 قطاعات بصور T1/T2: 9 · 53 · 11) → قيمنا → **الشهادات الثلاث ببطاقات مستقلة** (`.certs/.cert`) → IntentLinks. CSS في نهاية `v7.css`.
- **قهوجيات↔زواجات 57.0% → 44.9%:** `images.exclude` جديد في `IntentContent` (يمنع التعبئة الآلية من سحب صور الطاقم النسائي إلى صفحة الزواجات — الصور المشتركة 14→3)، `servicesP`/`rolesP` اختياريان يُغنيان عن فقرات القالب، بطاقات «خدماتنا في المدينة» عُلّمت `data-sim="chrome"` (تنقّل متطابق لا محتوى)، فقرة «جدول ليلة الزواج»، روابط مكمّلة مختلفة.
- **صفحات (خدمة × مدينة):** بنية جديدة `src/lib/local/{types,index,<city>}.ts` — `LOCAL_TEXT[city][service]` نص مكتوب يدوياً بزاوية المدينة المُقاسة من GSC (مكة 1,560 ظهوراً «قهوجي مكه» · الرياض 2,054 «صبابين» · الطائف 1,154 · الدمام 571 · أبها 417 · المدينة 136) مع `imageIds` مختارة و`guide` و`why` خاصة. `getServiceCityPage` يستخدمه إن وُجد وإلا `svcText` القديم. **مكتوبة:** مكة، الرياض، المدينة، الدمام، الطائف، أبها (18 صفحة). **متبقّية:** جدة وينبع (6 صفحات — ما زالت على القالب 90%).
- **القياس بعد الكتابة:** أزواج > 60% انخفضت 143 → 54؛ مكة↔جدة/الطائف/الرياض 35–42%؛ الأزواج المتبقية العالية كلها جدة↔ينبع (قالب) وصفحات /locations (86%).

# حالة الوكيل — ملف التسليم (HANDOFF) — لا ادّعاء غير مُقاس

> **لأي وكيل جديد: ابدأ من `AGENTS.md` في الجذر (ترتيب القراءة الكامل)، ثم هذا الملف، ثم `PLAN.md` و`docs/DECISIONS.md` كاملَين، ثم آخر 5 ملفات في `docs/owner-messages/`.**
> يُحدَّث في نهاية كل جلسة. كل رقم هنا خرج من أمر فعلي مذكور بجواره.

## آخر تحديث: 2026-09-11 (ج) — تنفيذ الإصلاحات التقنية بأمر المالك (D149–D155)

> **جديد 2026-09-11 (ج) — تنفيذ**: بأمر المالك «كوكيز ليش مافيش معانا · اصلح ايش داخلياً · ايش خراب وغير مرتب ثم نرجع للمحتوى»: **D149** خصوصية `/privacy` + شريط كوكيز + Consent Mode v2 (denied افتراضياً) + CSP pagead2 · **D150** sitemap lastmod حقيقي (2026-09-09/11) و`/links` خارج الخريطة · **D151** حذف motion/embla/DallahLogo · **D152** مجمّع الصور: المستهدف أولاً، 16 صورة مختلفة، لا تطابق شقيقات 100% (مباشرين جدة 2→16/16، مكة 4→15/15) · **D153** 12 تقرير جذر → `docs/archive/` + DEPLOYMENT.md جديد · **D154** حذف «أفضل» من الميتا الافتراضية + تصحيح @keifaldiafa · **D155** خطّا الأساس 185→178. البوابة: tsc ✓ lint ✓ build ✓ (44 مساراً) guard ✅ html-audit 43/43 · متصفح: 0 أخطاء على /privacy. **غير منشور بعد** — النشر بأمر المالك. **التالي:** المحتوى والصفحات (المرحلة 7) حسب أمر المالك.

## (ب) آخر تحديث السابق: 2026-09-11 (جلسة وكيل جديد — تحليل + بحث، لا تعديل كود)

> **جديد 2026-09-11**: `docs/reports/2026-09-11-project-history-deep-analysis.md` (تاريخ المشروع بأرقام) و`docs/reports/2026-09-11-competitors-and-global-standards-research.md` (المنافسون + معايير Google/Ads/GBP/PDPL + **13 فجوة مُقاسة G1–G13** مرتبة بالأثر مع صاحب القرار). أهم المكتشفات الجديدة (بعد مرور تحقق مستقل — سجل التحقق داخل كل تقرير): الإنتاج الحي ما زال الموقع القديم ✓ · لا سياسة خصوصية/موافقة كوكيز مع gtag GA4+Ads (PDPL) ✓ · lastmod كاذب ✓ · (asoulaldiafa = منافس عادي، نفى المالك أي علاقة). **مسحوب بعد التحقق**: «Pixels FB/TikTok محمَّلة» (إيجابي كاذب) و«TTFB 0.9s» (عابر). توكن المالك مضبوط في `~/.git-credentials` (خارج المستودع).
>
> **جديد 2026-09-11 (ب)**: `docs/reports/2026-09-11-structure-images-content-deep-analysis.md` — تحليل ثلاثي بأمر المالك من HTML المبني (43 صفحة): **الهيكلية سليمة** (0 روابط مكسورة · عمق ≤2 · 43/43 canonical/breadcrumb/og · `/links` يتيمة مقصودة لكنها في sitemap) · **الصور**: 272/314 مستخدمة، **42 لم تُستخدم قط** منها 8 إثبات T1–T2 كلها لصفحة `/coffee-break-sharikat-jeddah` غير المبنية؛ الصفحات الشقيقة تتشارك 60% (49–82%) من صورها؛ القالب المحلي يعرض 13 صورة فقط (ضيافة مناسبات جدة 52→18، مباشرين جدة 16→2)؛ alt فارغ 0، أحجام ✓ (متوسط 90KB، أقصى 178KB) · **المحتوى**: كلمات فريدة للمدينة **7–8%** فقط (Jaccard خام 49–50%)، 15 سؤال FAQ ×8 مدن، 0 رقم غير مؤكَّد، 0 ادّعاء تفوّق، 0 عنوان/وصف مكرر، `/about` أنحف صفحة جوهرية (210 كلمة/صورتان). القائمة المرتّبة في §4 — البنود 1–4 و9 قرار مالك، 5–8 تقنية صغيرة.

## 1. أين نحن — جملة واحدة
**البناء مكتمل (44 مساراً على النموذج v6.9 — منها `/privacy` D149) · التدقيق مكتمل (guard ✅ 178/178/11 · HTML 43/43 · آخر Playwright/axe 2026-09-09: 168/168 · 0) · التوثيق مكتمل (D109–D155 + 3 تقارير تحليل 2026-09-11) · الإصلاحات التقنية الداخلية مكتملة (D149–D155) · غير منشور.** المتبقي = قرارات المالك فقط: بداية المرحلة 7 (أ/ب — سؤال مطروح)، مراجعة الصور D139، مقاسات D138، أمر النشر. المتبقي = قرارات المالك (مراجعة الصور D139، مقاسات النموذج الصغيرة D138، أمر النشر) + دَين أداء يُقاس على الاستضافة الفعلية.

## 2. ما تم (مُقاس)
| المرحلة | الحالة | الدليل |
|---|---|---|
| 0 التثبيت | ✅ | PLAN §المرحلة 0 · **يدوي من المالك**: CI workflow (التوكن بلا صلاحية `workflows`) |
| 1 الصور والشعارات | ✅ | 314 صورة كتالوج · 49 شعاراً · CH9 |
| 2 الشِل | ✅ | Header/Footer/WhatsAppFab/Lightbox/Reveal — v7.css 1:1 |
| 3 الرئيسية | ✅ معتمدة من المالك | `owner-messages/2026-09-09-اعتماد-الرئيسية-واستمرار-الهيكلية.md` |
| 4 قالب الصفحة المحلية (33 صفحة) | ✅ | D127–D131 · **المحتوى مكرر بين المدن مؤقتاً** (D129/D130 — بقرار المالك) |
| 5 الصفحات العامة (8) + `/links` + `/privacy` (D149) | ✅ | D132–D138 · `/links` بُنيت في المرحلة 6 (D145 — كانت ناقصة ولم تُعلَّم) |
| 6 الأداء + الفحص الشامل | ✅ (بحدود الساندبوكس) | D140–D146 · `docs/reports/2026-09-09-phase-6-performance.md` |

**أرقام آخر بوابة (2026-09-11 بعد D149–D155):** tsc ✓ · lint ✓ · build ✓ (44 مساراً، First Load 87–104KB) · guard ✅ seo **178/178** · similarity **178/178** · assets **11/11** · HTML audit **43/43** · Playwright `/privacy` 0 أخطاء كونسول (CSP مُصحَّح).
**أرقام بوابة 2026-09-09 (قبلها):** guard seo 185/185 · similarity 185/185 · assets **11/11** · Playwright **168/168** (42×4) · axe **0** (42 صفحة) · HTML audit **43/43** · pixel-audit 0/0/0 · Lighthouse جوال: services **84** · contact **83** · qahwajiin-jeddah **76** · home **69–83** (متذبذب — gtag) · BP **100** · a11y **100** · SEO **100**.

## 3. ما بقي — بالترتيب (من يملك القرار) — النسخة الكاملة المفصّلة في `PLAN.md` §4½
| # | البند | من يقرر | ملاحظة |
|---|---|---|---|
| 0 | **بداية المرحلة 7 — سؤال مطروح الآن**: (أ) محتوى فريد للمدن الثماني (doorway: 7–8% كلمات فريدة، S12 92–95%) أم (ب) صفحة `/coffee-break-sharikat-jeddah` أولاً (87 صورة جاهزة، 8 إثبات T1–T2 معطّلة)؟ | **المالك** — قال «ثم نرجع إلى المحتوى والصفحات» | لا تبدأ قبل الجواب. نموذج الكتابة اليدوية: `getIntentPage` |
| 1 | **مراجعة الصور/المحتوى بطاقة‑بطاقة** (D139): ستوك زخرفي باسم تقديم (#307 «كركديه مثلج» = شاي أحمر) · QR/شعارات مكررة (#108) · 32 زخرفية في /offerings | **المالك** — «آخر شي الصور بعدما أقلك» | لا تُلمس قبل أمره |
| 2 | مقاسات النموذج الصغيرة على الجوال: `.tag` 10.9px · `.l2` 9.9px · `<small>` 9px · `.contact .actions` عمود واحد | **المالك** (D115 ↔ D138) | سؤال مطروح منذ 09-09 ولم يُجَب |
| 3 | `/about` نحيفة (210 كلمة · صورتان · لا مؤسس/شهادات/تقييمات GBP 4.5★) — E-E-A-T | **المالك** (مادة: صورة، قصة، شهادات) | تقرير التحليل الثلاثي §3.7 · G6/G7 |
| 4 | روابط سياقية من الصفحات العامة (/services /offerings /portfolio /about تخرج بـ2 روابط body فقط) إلى الصفحات المحلية | تقني + نصّ قصير من المالك | تحليل §1.2 S-3 |
| 5 | Lighthouse ≥90 جوال لم يتحقق في المحاكاة | تقني — يُقاس على الاستضافة الفعلية | D141 |
| 6 | تفعيل CI (`ci/quality.yml` → `.github/workflows/quality.yml`) | **المالك يدوياً** | توكن الوكيل بلا `workflows` |
| 7 | **النشر** (Vercel — `docs/DEPLOYMENT.md` 2026-09-11) + إبطال التوكن المكشوف | **أمر المالك الصريح فقط** | أكبر رافعة فهرسة (G1) — الحي ما زال الموقع القديم |

**مُغلق 2026-09-11 (لا يُعاد فتحه):** G3 خصوصية/كوكيز (D149) · G4 lastmod (D150) · G13 تبعيات (D151) · صور القالب المحلي مكررة/مستعارة (D152) · G12 جذر/DEPLOYMENT (D153) · «أفضل» + @keifdiafa (D154) · G8/G11 مسحوبتان (إيجابيات كاذبة) · G9 pixels غير محمَّلة (صحيح — ENV غير مضبوط، خيار المالك).

## 4. أوامر التحقق (تعمل في هذا المستودع)
```bash
cd /home/user/webapp
npx tsc --noEmit && npm run lint && npm run build && npm run guard          # بوابة الجودة
# الخادم: بعد كل build اقتل الماسك للمنفذ ثم شغّل
ps aux | grep "[n]ext-server" ; kill <pid> ; (setsid nohup npx next start -p 3111 > /tmp/next.log 2>&1 &)   # لا pkill -f من داخل أداة Bash
curl -s http://localhost:3111/sitemap.xml | grep -o "<loc>[^<]*</loc>" | sed 's#<loc>##;s#</loc>##;s#https\?://[^/]*##' | sort -u > /tmp/all-paths.txt
node scripts/html-audit.mjs        # canonical/robots/JSON-LD/h1/og — 43/43
node scripts/browser-sweep.mjs     # 4 مقاسات + axe — يكتب /tmp/pall.log (يحتاج playwright/axe-core: في الساندبوكس روابط رمزية إلى /tmp/pw/node_modules)
node scripts/pixel-audit.mjs       # نص ضيق/صور صغيرة/أزرار متراصة/تمدد @390
CHROME_PATH=$HOME/.cache/ms-playwright/chromium-1134/chrome-linux/chrome npx lighthouse http://localhost:3111/services --form-factor=mobile --screenEmulation.mobile --throttling-method=simulate --output=json --output-path=/tmp/lh.json
```

## 5. دروس تشغيلية (لا تتكرر)
- **قارن بوابة خروج كل مرحلة بقائمة PLAN حرفياً** — `/links` سقطت من المرحلة 5 لأن الإغلاق كان من الذاكرة (D145).
- **لا تعتمد على DOM/axe فقط**: بطاقة الخدمة «مرّت» كل الفحوص وهي مكسورة بصرياً. بعد كل CSS: pixel-audit + لقطة عنصر تُقرأ بالعين (D137/D138).
- نقل CSS من النموذج: الأسطر الهيكلية (`@media`, `}`) تُحفظ دائماً، ثم عدّ الأقواس (764/764 الآن).
- بعد كل `npm run build`: أعد تشغيل خادم 3111 (خادم قديم + HTML جديد ⇒ 400 على chunks). وإن ظهرت أخطاء `.next/types` في tsc بعد نقل مسارات: `rm -rf .next/types && npm run build` أولاً.
- مكوّنات العميل لا تستورد وحدات المحتوى (تجرّ الكتالوج 172KB) — props من الخادم (D140).
- `brokenImgs` في السويب على خادم بلا CDN = إيجابيات كاذبة محتملة؛ أعد الفحص بانتظار `load` قبل الحكم (D146).
- `pkill -f <اسم>` من داخل الـBash tool يقتل الجلسة نفسها إن طابق أمرها — استخدم pid.

- **تحقق ثانٍ لكل ادّعاء تحليلي قبل كتابته** (09-11: «pixels محمَّلة» كان regex على حارس `typeof fbq`؛ «TTFB 0.9s» كان عينة باردة واحدة). كل تقرير يحمل سجل تحقق.
- `grep` بنمط عربي على HTML كبير قد يُرجع 0 زوراً (locale) — استخدم python للعدّ.
- بعد تغيير `poolFor`/الصور: أعد قياس التطابق بين الشقيقات **على الصور المتغيّرة فقط** (الثابت 21–22 صورة من القالب يرفع Jaccard الكلي إلى ~60% مهما فعلت).

## 6. بنية المسارات (بعد D145/D149)
- `src/app/(site)/` = كل صفحات الموقع + `template.tsx` (ClientLayout: هيدر/فوتر/fab/Lightbox/Reveal/PageFx) + `loading.tsx`
- `src/app/(bare)/` = `layout.tsx` بلا شِل + `links/page.tsx` (index,follow — خارج sitemap D150)
- `src/app/(site)/privacy/` = سياسة الخصوصية (D149) · `src/components/CookieConsent.tsx` شريط الموافقة (يُركَّب في `layout.tsx` الجذر)
- `src/app/` جذر: `layout.tsx` (خطوط/ميتا/gtag مؤجّل/شِيمات) · `not-found.tsx` (يلفّ ClientLayout صراحةً) · robots/sitemaps
- مخرجات `.next/server/app/*.html` لا تتأثر بمجموعات المسارات — الفاحصات تعمل كما هي.

## ⏱ تحديث 2026-09-18 (جلسة المرحلة 7 — الجزء الأول) — Claude (Anthropic) · وكيل GenSpark AI Developer

**أُنجز وبُني وفُحص (guard ✅ seo 192/192 · similarity 192/192 · assets 11/11 · tsc ✓ · lint ✓ · build 56 مساراً):**
- D156 منهجية المرحلة 7 · D157 تحديث خط الأساس 178→192 (16 زوجاً قديماً تحرّك 60.0–61.2% بعد تصحيح الإملاء؛ لا زوج يشمل الصفحات الجديدة).
- فحص S15 في `scripts/seo-guard.mjs`: يمنع «قهوجيون/صبابون/مباشرون» (0 بحث مُقاس) — 13 موضعاً صُحّحت في src.
- بنية صفحات النيّة: `src/lib/intent/{types,index,<slug>}.ts` (محتوى يدوي لكل صفحة) · `getIntentPage` معمّم · `IntentRoute.tsx` (ميتا + Breadcrumb/Service/WebPage/ImageGallery) · قالب LocalPage: pts/badge3/worksH2 قابلة للتخصيص + قسم «الدليل» (#guide) + «المعرض الإضافي» (#gallery .pgrid) + التغطية بروابط (#districts) · CSS `.guide .gd`.
- **الصفحات الخمس مبنية:** `/qahwajiyat-sababat-jeddah` (2,010 كلمة · 46 صورة · تشابه أقصى 57.0% مع زواجات) · `/diyafa-alyawm-alwatani` (2,105 · 45 · 49.8%) · `/qahwajiin` عام بلا مدينة (2,042 · 45 · 50.3%) · `/coffee-break-sharikat-jeddah` (2,193 · 63 · 49.8%) · `/diyafa-a3ras-jeddah` (2,143 · 54 · 57.0%). كلها في INTENT_PAGES → sitemap (lastmod 2026-09-18) · LocationsPage · EXISTING_PAGES (صورها انتقلت من /portfolio تلقائياً).

**لم يُنفَّذ بعد (بالترتيب):**
1. html-audit على `next start -p 3111` + لقطات 390/768/1024/1440 للصفحات الخمس (D117) — **لم تُلتقط**.
2. خفض تشابه قهوجيات↔زواجات 57% → ≤45% (تقليل تكرار فقرات القسم النسائي في صفحة الزواجات).
3. إغناء `/qahwajiin-makkah` والـ24 صفحة (خدمة×مدينة) بنصوص فريدة — أكبر دَين تشابه (95%).
4. إعادة كتابة `/about` + إبراز الشهادات الثلاث؛ روابط سياقية من /services /offerings /portfolio إلى الصفحات الجديدة؛ قسم «أماكن نصل إليها» في صفحات جدة.
5. تحديث AGENTS.md §3 · PLAN.md §5 · DEPLOYMENT.md (Vercel=keif-v2).
6. النشر الكامل (أمر المالك — بعد اكتمال كل شيء) + طلب فهرسة GSC.

## ⏱ تحديث 2026-09-18 (جلسة المرحلة 7 — الجزء الثاني: تدقيق شامل) — Claude (Anthropic)

**أُنجز (guard ✅ 192/192/11 · tsc ✓ · lint ✓ · build 56 مساراً · مدفوع إلى main):**
- تدقيق بصري 20 تصييراً (5 صفحات × 390/768/1024/1440): 0 فيض · 0 صور مكسورة · 0 بلا alt · 0 أخطاء — لقطات في `docs/reports/shots-2026-09-18/`.
- Lighthouse جوال: /qahwajiin 90/100/100/100 · الصفحات ذات gtag محمَّل 60–64 (TBT من gtag لا الصور) — قرار D141 بعد النشر.
- الصور: 0 صورة >300KB، تسليم AVIF ≤93KB — لا ضغط إضافي مطلوب.
- الخرائط: 48 رابطاً + 274 صورة/12 صفحة + index (lastmod 2026-09-18).
- **Ads:** تشريح 3,000 ريال — تقرير (ج) `docs/reports/2026-09-18-ads-3000-forensics-and-plan.md` + خطة 4 مراحل بانتظار (أ/ب). خام في `data/search-intel/2026-09-18/ads-campaign-2026-*.json`.
- D158 أحياء جدة بالطلب المُقاس (18 حياً) · D159 معجم القهوة السعودية (saudipedia) مُدخل في صفحتين.
- تقرير (د) التكامل: `docs/reports/2026-09-18-visual-perf-images-sitemaps-integration.md` — «الجديد جاهز، الموقع ككل ليس 100% قبل الـ24 و/about».

**درس تشغيلي (§5):** بعد `rm -rf .next && npm run build` اقتل خادم `next start` القديم بـ `pkill -9 -f next` وإلا بقي يخدم البناء المحذوف (ظهرت 404 كاذبة للصفحات الجديدة).

**المتبقي (بالترتيب):** (1) نصوص فريدة للـ24 صفحة (خدمة×مدينة) — التكرار 95% · (2) /about بالشهادات الثلاث · (3) روابط سياقية من /services /offerings /portfolio · (4) خفض قهوجيات↔زواجات 57%→≤45% · (5) تحديث AGENTS §3 · PLAN §5 · DEPLOYMENT (Vercel=keif-v2) · (6) النشر بأمر المالك ثم تنفيذ خطة Ads (أ) أو تسليم ملف (ب) · (7) قياس Lighthouse على الرابط الحيّ.

---

# 🔴 تسليم جلسة 2026-09-18 (الجزء 3) — «كمّل بدقة» → توقّف بأمر المالك (رسالة 11: «وثّق كل شي وارفع — 10 دقائق»)

**اقرأ هذا القسم أولاً. الحالة: كود مرفوع على `main` لكنه لم يمرّ بالبوابة الكاملة بعد آخر تعديل.** آخر بناء ناجح (`npm run build` ✓ · `tsc` ✓ · `lint` ✓) لكن **seo-guard فيه مخالفات جديدة في S1/S4** (أطوال عناوين/أوصاف) في ملفات `src/lib/local/*.ts` — يجب إصلاحها قبل النشر.

## ما أُنجز في هذه الجلسة (D160) — مرفوع
1. ✅ **روابط سياقية** إلى صفحات النيّة الـ5: `IntentLinks` في /services /offerings /portfolio /about + شريط «وبحسب المناسبة» في الرئيسية.
2. ✅ **/about أُعيدت كتابته بالكامل** + الشهادات الثلاث ببطاقات (`AboutPage.tsx`، CSS آخر `v7.css`، metadata في `app/(site)/about/page.tsx`).
3. ✅ **قهوجيات↔زواجات: 57.0% → 44.9%** (هدف ≤45% تحقّق). آلية: `images.exclude` + `servicesP/rolesP` + بطاقات المدينة chrome.
4. 🟡 **نصوص يدوية (خدمة×مدينة):** بنية `src/lib/local/` جاهزة ومربوطة في `getServiceCityPage`. **6 مدن مكتوبة** (مكة، الرياض، المدينة، الدمام، الطائف، أبها = 18 صفحة، تشابه مع الشقيقات 35–42%). **جدة وينبع لم تُكتبا** (6 صفحات على القالب القديم ~90%).

## 🔧 ما يجب إصلاحه فوراً في الجلسة القادمة (بالترتيب)
1. **S1/S4 في `src/lib/local/*.ts`:** العنوان الخام يجب ≤46 حرفاً (اللاحقة « | كيف الضيافة» تضيف 14 → الحدّ 60). الوصف 120–158 حرفاً (استبدل `${WA}` بـ 0508252134 عند القياس). الحالة الحالية المُقاسة:
   - عناوين >46: `abha` الثلاثة (66/64/68 مع اللاحقة) · وربما `taif`/`dammam`/`madinah`/`makkah`/`riyadh` — **سكربت التصحيح فشل في منتصفه** (assert) فبعض الملفات تعدّلت وبعضها لا. افحص كلها: `grep -n 'title: "' src/lib/local/*.ts`.
   - أوصاف خارج النطاق: `taif` diyafa 91 (<120) · `abha` sababin 160 · `abha` diyafa 165 · `riyadh` diyafa 110 · `madinah` diyafa 104. (سكربت القصّ السابق قصّ زيادة في بعضها — أعِد كتابة هذه الأوصاف يدوياً 120–158.)
2. **كتابة جدة وينبع** في `src/lib/local/jeddah.ts` و`yanbu.ts` على نفس نمط `makkah.ts` (3 خدمات × {kicker,h1,title,desc,intro,pts,badge3,servicesP,imageIds,roles×3,packages×3,packagesP,guideH2,guideP?,guide×4,why×6,faqs×6,districtsH2}) وتسجيلهما في `src/lib/local/index.ts`. المُقاس لجدة (GSC 16 شهراً): «صبابين قهوة بجدة» 273 · «صبابين قهوة جدة» 261 · «قهوجية ومباشرين في جدة» 174 · «قهوجيين جدة» 126 · «صبابات قهوة جدة» 121 · «مباشرات ضيافة جدة» 115. ينبع: «قهوة ينبع» 23 · «ضيافة ينبع» 7 (طلب ضعيف — نص أقصر مقبول). جدة عندها الكتالوج الأغنى (28/25/52 صورة مستهدفة).
3. **تحديث خط الأساس بعد الإصلاح:** `UPDATE_BASELINE=1 node scripts/seo-guard.mjs` و`UPDATE_BASELINE=1 node scripts/similarity-check.mjs` (الأزواج >60% نزلت من 143 إلى 54 — انخفاض حقيقي، وثّقه في قرار D161 كما فُعل في D157). ثم `npm run guard` أخضر.
4. **الشقيقات الداخلية للمدينة الواحدة** (مثل qahwajiin-makkah↔sababin-qahwa-makkah = 45.1%) على الحدّ — مقبولة لكن راقبها.
5. **صفحات /locations/<city>** ما زالت 80–86% متشابهة (قالب `getCityPage`) — المرشّح التالي بعد جدة/ينبع.
6. **الذاكرة:** `AGENTS.md §3` · `PLAN.md §5` · `docs/DEPLOYMENT.md` (Vercel = keif-v2) لم تُحدَّث في هذه الجلسة — حدّثها.
7. **لقطات + html-audit + تقرير «جاهز للنشر»** بعد اخضرار البوابة.

## أدوات مؤقتة مفيدة (أعِد إنشاءها إن لزم — كانت في /tmp خارج الريبو)
- `/tmp/simpair.mjs a b` — يقيس زوجاً واحداً من `.next/server/app/*.html` بنفس خوارزمية `scripts/similarity-check.mjs` ويطبع الكلمات المشتركة (يستورد `extractVisibleText, neutralize, ratio`).
- `/tmp/common.mjs a b n` — يطبع العبارات المشتركة الحرفية ≥n كلمات (ما يُعاد كتابته لتقليل التشابه).

## ملاحظات تشغيلية
- `next start -p 3111` قد يكون قديماً — اقتله قبل الفحص البصري (`ps aux | grep next | xargs kill -9`).
- OAuth Google في `/tmp/gcreds` مؤقت — سينتهي؛ لا شيء منه في الريبو.
- المالك: لا PR — رفع مباشر على `main`. كل رسالة تُحفظ حرفياً في `docs/owner-messages/`. الأرقام المسموحة فقط: +500 · منذ 2016 · الرقم الموحّد · واتساب. S15: قهوجيين/صبابين/مباشرين لا قهوجيون.

## المُعلَّق على المالك (لم يُجب بعد)
- خطة Ads (تقرير `docs/reports/2026-09-18-ads-3000-forensics-and-plan.md` §8): (أ) تنفيذ عبر API أم (ب) ملف؟
- النشر على keif-v2 + طلب فهرسة GSC بعد النشر.

---

# ⏱ جلسة 2026-09-22 (الجزء 1 — تهيئة + بحث، لا تعديل كود) — Claude (Anthropic) · وكيل GenSpark AI Developer

**البوابة عند الفتح (مُقاسة):** tsc ✓ · lint ✓ · build ✓ **56 مساراً** · seo-guard **❌ 59/192 مع 5 جديدة** (S1 أبها ×3 عناوين 64–68 · S4 أبها ×2 أوصاف 160–165) · similarity ✅ 54/192 · assets ✅ 11/11 · html-audit ✅ **48/48** (sitemap 48). **تنبيه:** `npm run guard` يُرجع exit 0 رغم فشل seo — شغّل `node scripts/seo-guard.mjs` منفرداً للتأكد. جدة↔ينبع = أعلى 3 أزواج في الموقع (90.9 · 90.3 · 86.5%).

**بأمر المالك «قبل أن تبدأ حلّل المنافسين والمعايير والبحوث الأكاديمية وفهرسة AI»:** تقرير `docs/reports/2026-09-22-competitors-google-standards-academic-geo-research.md` (سجل تحقق 20 بنداً) + **D161** قواعد كتابة W1–W9. أهم الجديد: thawq-rafeea #1 على «قهوجيين» بschema غنيّ · Google: GSC generative-AI control/report لكل المواقع منذ 08-31 · Preferred Sources في AI Mode · Ads message assets→واتساب + إزالة استهداف اللغة (سبتمبر 2026) · أكاديمياً: إحصاءات/اقتباسات +32–41% (KDD 2024) لكن المراجعة النقدية 2026 تحذّر: الصلة والموضع فقط قابلان للتكرار، وإعادة الكتابة «للـAI» قد تُنقص الاسترجاع 16%.

**رسائل المالك المحفوظة اليوم (3):** `2026-09-22-فتح-جلسة-*` · `2026-09-22-البرمت-قديم-*` · `2026-09-22-قبل-البدء-حلل-المنافسين-*`.

**التالي (بانتظار «ابدأ» من المالك):** قائمة (أ) 1→7 كما في تقرير الجاهزية + تطبيق D161 أثناء الكتابة + T1–T4 من التقرير §7ب. قرارات مالك مفتوحة: A1 GSC control · A2 Search profile · A3 founder Person · A4 message asset واتساب · A5 Earned media · خطة Ads (أ/ب) · مراجعة الصور D139 · النشر.


## ⏱ جلسة 2026-09-22 (الجزء 2) — تنفيذ «انطلق بدقة وعمق» — D162
- **الحالة**: البوابة خضراء بعد بناء نظيف: tsc ✓ lint ✓ build 56 ✓ · seo-guard exit 0 (28، 0 جديدة) · similarity 28 ≤71.36% · assets 11 · html-audit 48/0. خطّا الأساس **192 → 28** (`UPDATE_BASELINE=1`، موثّق D162).
- **ما تغيّر**: `src/lib/local/jeddah.ts` + `yanbu.ts` (جديدان، مسجّلان في `index.ts` — المدن الثماني كلها يدوية الآن) · `cities.ts` مكتوب من جديد للثماني · `localPage.ts` getCityPage يقرأ الأدوار من النص اليدوي · `schema.ts` T2 · `site.ts` `SOCIAL.googleReview` · About/Contact T3 · `LocalPage.tsx` + 4 intent H2 وصفية (T4) · abha/madinah/riyadh/taif S1/S4.
- **التقرير**: `docs/reports/2026-09-22-phase7-ready-to-publish.md`.
- **لم يُنفَّذ**: لقطات الشاشة 4 أعراض (playwright غير مركّب — `npx playwright install chromium` ثم `/tmp/shot.mjs` منطقه موثّق في التقرير) · T1 (يفحصه المالك) · T5 بعد النشر.
- **تذكير تشغيلي**: `npm run guard` يُرجع 0 حتى لو فشل seo-guard — شغّل `node scripts/seo-guard.mjs` وحده. البناء قد يفشل بـENOTEMPTY إن كان `next start` يعمل على .next — أوقفه أو أعد المحاولة.
- **المعلّق على المالك**: أمر النشر · A1–A5 · Ads أ/ب · D139.

# تقرير النشر — D173 — 2026-09-23 06:20 UTC

**المؤلف:** Claude (Anthropic) · **بأمر المالك:** «يالله ابدأ النشر بدقة… انشر باسم MoTechSys كي ينشر فيرسال… ووثق ايش باقي».

## 1. الاكتشاف الحاسم (قبل النشر)
مشروع Vercel لـ `keifaldiafa.com` **مربوط بـ `MoTechSys/keif-v2` فرع `main`**، لا بـ `keif-aldiafa-web`. الدليل: بعد رفع نسختنا إلى keif-v2 (`358a19f` — الساعة 05:47) صار الموقع الحي يخدم D171 (og/home.webp) وD166 وD167 تلقائياً، بينما `bc6edeb` المدفوع إلى keif-aldiafa-web فقط (llms.txt) بقي 404 على الإنتاج حتى دُفع إلى keif-v2.
**قاعدة النشر من الآن:** `keif-aldiafa-web/main` = مصدر الحقيقة والتطوير · **النشر = دفع نفس الـcommit إلى `keif-v2/main`** (بلا `.github/workflows/quality.yml` لأن رمز الوكيل بلا صلاحية workflows). موثّق في `DEPLOYMENT.md` و`.github/WORKFLOW-NOTE.md` داخل keif-v2.

## 2. ما نُشر
| | |
|---|---|
| المصدر | `keif-aldiafa-web` `04b4664` (= D149…D172 كاملة) |
| الهدف | `keif-v2/main` `3c3d1a8` (نفس الشجرة − workflow + WORKFLOW-NOTE.md) |
| المنصّة | Vercel (iad1) — بناء تلقائي، اكتمل بعد ~80 ثانية من الدفع (06:19:13 → 06:20:33) |
| القديم | محفوظ: `keif-v2` فرع `archive-v2-old-site-2026-07-25` + `keif-aldiafa-web` فروع `old-version-2026-09-0x` |

## 3. فحص ما بعد النشر (الإنتاج الحي، curl + Playwright جوال 390×844)
| الفحص | النتيجة |
|---|---|
| الموقع الجديد حيّ | `v7-header` موجود ✅ · بطاقات D166 ✅ · CSS D167 ✅ |
| المسارات الـ49 | **49/49 = 200** (كانت `/links` `/privacy` `/mubashirin-qahwa-jeddah` 404 على القديم) |
| ملفات الجذر | robots · sitemap · sitemap_index · image-sitemap · **llms.txt** · manifest · favicon · og/*.webp = 200 · صفحة غير موجودة = 404 ✅ |
| التحويلات | `www` → 301 non-www ✅ · `http` → 308 https ✅ |
| robots | يذكر GPTBot/ClaudeBot/PerplexityBot ✅ |
| og:image لكل صفحة | `/`→home · `/qahwajiin-jeddah`→qahwajiin-jeddah · `/portfolio`→portfolio ✅ · واتساب UA يجلب WebP 85KB = 200 ✅ |
| رؤوس الأمن | HSTS · CSP · X-Frame ✅ |
| أخطاء console/شبكة (4 صفحات، جوال) | **0** |
| الكوكيز vs أزرار الهيرو (الرئيسية) | الشريط 697 · الأزرار 684 → لا تغطية ✅ |
| تمرير أفقي | 0 ✅ |

## 4. ما يجب أن يفعله المالك الآن (لا يستطيع الوكيل)
1. **مسح كاش المعاينات** كي تظهر البطاقات الجديدة فوراً: Facebook Sharing Debugger → Scrape Again للرئيسية وأهم 5 صفحات · تيليجرام: أرسل الروابط إلى `@WebpageBot` · واتساب: يتجدّد تلقائياً خلال ~7 أيام (أو أرسل الرابط بـ `?v=2` مؤقتاً).
2. **من هاتف حقيقي:** زر واتساب · الاتصال · الباركود → `/links` · شريط الكوكيز يظهر مرة واحدة.
3. **Search Console:** أرسل `https://keifaldiafa.com/sitemap_index.xml` واطلب فهرسة `/` و`/qahwajiin-jeddah` و`/sababin-qahwa-jeddah` و`/portfolio` (أو أعطِ الوكيل التوكن ليفعلها).
4. **Google Ads:** الروابط النهائية للحملة → الصفحات الجديدة (أو التوكن للوكيل).
5. **PageSpeed Insights (جوال)** بعد 24–48 ساعة للرئيسية و`/qahwajiin-jeddah` — أرسل النتيجة للوكيل.
6. **CI:** في keif-v2 انسخ `ci/quality.yml` → `.github/workflows/quality.yml` من واجهة GitHub.

## 5. ما يبقى للجلسة القادمة (محصى)
| # | البند | يحتاج |
|---|---|---|
| 1 | `aggregateRating` في Schema | عدد ومتوسط تقييمات Google من المالك |
| 2 | Search Console: فهرسة + تحقق التغطية · Ads: توجيه الحملة | توكنات المالك |
| 3 | قياس CWV الحقيقي (PSI) ومعالجة ما يظهر | نتيجة PSI |
| 4 | دمج 10 أزواج سجلات صور مكررة · #307/#93 | قرار المالك |
| 5 | طول الصفحات (13–21 شاشة) — تقصير المحتوى | قرار محتوى |
| 6 | نسخة إنجليزية/hreflang | قرار كبير |
| 7 | `security.txt`/`humans.txt` | صغير — بأمر |
| 8 | ترقية مسار النشر: إضافة remote `kv2` في خطوات النشر أو ربط Vercel بـ keif-aldiafa-web مباشرة (أنظف) | قرار المالك في لوحة Vercel |
| 9 | استعادة `/tmp/kv2src` (الأصول النظيفة) و`/tmp/yunet.onnx` عند الحاجة لإعادة توليد OG/الختم — المصدر: فرع `archive-v2-old-site-2026-07-25` في keif-v2 | تلقائي عند الحاجة |

## 6. D174 — أداء الجوال على الإنتاج (نُشر 17:20 UTC، keif-v2 `8fe3a12`)
**القياس قبل (Lighthouse 12، جوال، الإنتاج):** الرئيسية 53 · جدة 61. TBT 2.3s/1.5s · TTI 7.5s. السبب المحدَّد من الـtrace: gtag (347KB، 947+539ms خيط رئيسي) يُحمَّل عند أول `scroll` أو بعد 2.5s = داخل نافذة القياس؛ و9 طلبات `?_rsc=` (~190KB) من prefetch روابط الرأس/الفوتر/الرئيسية أثناء التحميل.
**التنفيذ:** إزالة `scroll` من محفّزات gtag + الخمول 4s (اللمس/النقر/الكتابة تُحمِّله فوراً) · `prefetch={false}` على 32 رابط Link (الرأس 2 · الفوتر 7 · الرئيسية 9 · المحلية 10 · المشتركة 4) — Next يُحمِّل عند hover/لمس فلا يتأخر التنقّل الفعلي.
| | قبل | بعد |
|---|---|---|
| الرئيسية perf | 53 | **78** |
| جدة perf | 61 | **79** |
| LCP | 4.2s / 3.6s | **2.6s / 2.6s** |
| TBT | 2,280 / 1,480ms | **680 / 750ms** |
| TTI | 7.5 / 7.3s | **3.5 / 3.4s** |
| Speed Index | 5.2 / 4.7s | **3.8 / 2.1s** |
| A11y / BP / SEO | 100/100/100 | 100/100/100 |
**التحقق:** gtag محمَّل عند 5.5s وعند أول لمسة على 3 صفحات ✅ (لا فقدان لقياس GA4/تحويلات Ads) · 49/49 مسار 200 · 0 أخطاء · `security.txt` `humans.txt` 200.
**ما يبقى للأداء (يحتاج قرار/قياس ميداني):** LCP 2.6s على حدّ «جيد» (≤2.5) — التحسين التالي: خفض حجم JS الأولي (~490KB؛ `Strip`/`Lightbox` عميل) أو تأخير شرائط الصور تحت الطية؛ TBT 680ms هدفه ≤200 (INP) — يُقاس بـ CrUX الحقيقي بعد 28 يوماً في PSI/Search Console.

/**
 * حزمة ميزات الحركة الكسولة — المرحلة 3 من خطة الأداء (2026-08-29)
 *
 * لماذا هذا الملف؟
 * كانت مكتبة motion كاملة (~135KB، حزمة 148) تُحمَّل بشكل متزامن في كل صفحة
 * وتستهلك 3.2 ثانية على الخيط الرئيسي (قياس Lighthouse موبايل).
 *
 * الحل: LazyMotion يفصل «مكوّن m الخفيف» (يبقى في الحزمة الرئيسية)
 * عن «محرك الميزات» (هذا الملف — يُحمَّل كسولاً بعد الترطيب/hydration
 * خارج المسار الحرج للعرض الأول).
 *
 * نستخدم domMax (وليس domAnimation الأصغر) لأن المشروع يعتمد على:
 * - drag="x" في offerings/portfolio/services (سحب المعارض)
 * - layoutId="activeIndicator" و"nav-pill" (انتقالات التخطيط)
 * وهذه تتطلب محرك projection المتوفر فقط في domMax.
 */
import { domMax } from "motion/react";

export default domMax;
